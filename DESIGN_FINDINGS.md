# 기능 테스트 설계 발견사항

작성일: 2026-07-25  
대상 버전: 0.1.0

## 검증된 흐름

| 항목 | 결과 |
|---|---|
| 2~10명 명단 파싱 | 통과 |
| 11번째 이름 보존 및 시작 차단 | 통과 |
| 중복 이름의 별도 참가자 처리 | 통과 |
| 결과 선확정형의 시드 재현 | 통과 |
| 물리 결과형의 시드 재현 | 통과 |
| 같은 경기 다시 보기 | 같은 우승자·순위 재현 |
| 데스크톱 1600×900 | 시작 행동과 경기 HUD 확인 |
| 모바일 390×844 | 준비·10인 경기·결과 확인 |
| 결과 기록 | 브라우저 로컬 저장, 최대 20개 |

## 발견 1: 전원 물리 완주를 보장할 수 없음

10인 실전 브라우저 테스트에서 8명이 결승선을 통과하고 2명이 제한 시간까지 코스에 남았습니다. 이는 시뮬레이션 오류가 아니라 장애물과 충돌하는 물리 코스에서 자연스럽게 생길 수 있는 정체입니다.

현재 규칙:

- 제한 시간: 시뮬레이션 기준 52초
- 완주자: 결승선 통과 순서
- 미완주자: 제한 시점의 세로 진행 거리, 중앙선 근접도 순
- UI: 결과 화면 `실행 정보`에 `물리 완주 8/10 · 제한 시간 순위 적용`처럼 공개

다음 결정:

1. 스포츠형: 현재 규칙을 유지하고 DNF/진행 거리 판정을 전면에 표시
2. 방송형: 참가자와 무관한 전역 진동·중력 변화로 전원 완주율을 높임
3. 쇼형: 첫 결승 통과에서 경기를 종료하고 우승자만 공식 결과로 취급

현재 구현은 1번과 3번의 중간 형태입니다. 화면 연출은 첫 결승 통과 직후 끝나지만 전체 순위는 52초 물리 계산 결과를 사용합니다. 다음 버전에서는 한 규칙으로 통일하는 것이 좋습니다.

## 발견 2: 결과 선확정은 물리적으로 합리적인가

가능합니다. 다만 “선택된 사람의 마블을 중간에 밀어준다”는 방식은 배제해야 합니다. 현재 구현은 익명 슬롯 경기를 먼저 완성한 뒤, 잠근 참가자 순위를 슬롯의 최종 물리 순서에 배정합니다.

이 방식의 장점:

- 경기 중 개별 힘 보정이 없음
- 동일 시드는 동일 궤적과 결과를 재현
- 참가자 이름과 색상이 물리에 영향을 주지 않음
- 시청자가 보는 충돌과 추월이 실제 궤적과 일치

남는 쟁점:

- 공정한 추첨인지, 연출된 결과 공개인지 UI에서 명확히 구분해야 함
- 선확정 모드를 일반 사용자에게 노출할지 운영자 설정으로 숨길지 결정 필요

## 발견 3: 스타트 라인 이동

현재 `자동 배치 다시 만들기`는 모든 마블의 출발 묶음을 같은 양만큼 좌우 이동합니다. 개별 마블 이동은 허용하지 않습니다.

- 허용: 전체 랙의 제한된 좌우 이동
- 금지: 특정 참가자만 유리한 슬롯으로 이동
- 잠금 시점: 경기 시작 버튼을 누른 순간
- 재현 정보: 결과 화면에 출발 이동값과 레이아웃 시드 저장

## 다음 버전 우선순위

1. 경기 종료 규칙을 `첫 우승자 확정` 또는 `전원 완주/명시적 DNF` 중 하나로 확정
2. 여러 코스 시드에서 완주율·평균 경기 시간·스타트 슬롯 편향을 자동 집계
3. 결과 선확정형을 운영자 전용 설정으로 분리할지 결정
4. 효과음을 실제 충돌·관문·결승 이벤트 단위로 확장
5. 레또룰렛 통합 전 `RunRequest`와 `GameResult` 어댑터만 별도 패키지로 추출
