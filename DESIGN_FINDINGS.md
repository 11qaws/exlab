# 기능 테스트 설계 발견사항

작성일: 2026-07-26

대상 버전: 1.2.0

## 결론

1.2.0은 결과 선확정과 순위 기반 보정을 제거하고, 출발 슬롯 배정 이후의 모든 결과를 실제 Matter.js 물리로 결정한다. 결승부는 전체 대열을 붙잡던 양측 점프 범퍼를 없애고 한쪽 위험 범퍼와 벽 방향 회전 막대만 남겼다. 최종 24경기 홀드아웃에서 결승 진입 선두 우승률은 이전 20.8%에서 목표값 75.00%로 올라 레이스 전반의 의미를 회복했고, 25.00%의 마지막 반전 가능성을 유지했다.

## 발견 1: 물리 전용 결과는 슬롯 배정 시점부터 시작해야 한다

결과를 먼저 정한 뒤 최종 물리 슬롯에 참가자 이름을 대응하는 방식은 경기 중 힘을 조작하지 않더라도 운영자가 결과를 잠갔다는 사실을 숨길 수 있다. Race는 이 모드를 완전히 제거했다.

현재 계약:

- `createRaceSlotAssignment`가 `raceSeed`로 참가자와 익명 출발 슬롯을 경기 전에 한 번 배정
- `layoutSeed`로 출발 묶음 전체의 제한된 좌우 이동 결정
- 참가자·슬롯 대응은 방송 대기 전에 잠금
- `simulateRace`가 확정 매핑을 입력받아 `RaceSimulation.slotToCandidateId`에 보존
- `buildRacePlan`은 시뮬레이션의 매핑을 검증·사용하며 별도 슬롯 대응을 다시 생성하지 않음
- 결과 시드, 사전 우승자 목록, 결과 모드 선택 없음
- 경기 중 참가자 ID, 이름, 색, 현재 순위, 선두 거리, 예정 결과를 힘 계산에서 사용하지 않음
- 실제 결승 통과 순서만 당첨자와 순위를 결정

같은 두 시드는 같은 배치와 프레임을 재현하지만 특정 참가자의 승리를 별도 조건으로 보장하지 않는다.

## 발견 2: 높은 탄성은 수동 속도 보정보다 읽기 쉽다

이전 능동 범퍼는 접촉 뒤 속도를 직접 덮어써 마블을 위쪽·안쪽으로 발사했다. 강한 반전은 만들었지만 Matter의 충돌 결과와 별개인 응답이라 시청자가 장애물 형상만 보고 움직임을 예상하기 어려웠다.

1.2.0은 수동 위치·속도 변경을 삭제하고 Matter의 접촉 법선과 반발계수만 사용한다. 빠른 마블이 벽을 통과하는 문제는 경계 밖 좌표를 강제로 되돌리지 않고 60Hz 경기 스텝마다 두 번의 120Hz 물리 서브스텝을 계산해 예방한다.

| 장애물 | 반발계수 | 의미 색 | 관찰되는 역할 |
|---|---:|---|---|
| 막대형 범퍼 | `1.22~1.32` | 핫핑크 | 강한 횡반사와 마블 재충돌 |
| 핀 | `0.48~0.62` | 레몬 | 작은 방향 분기 |
| 가이드·디플렉터 | `0.40~0.51` | 민트 | 중앙 하류 유도 |
| 탄성 벽 | 범퍼와 동일 | 스카이 | 벽 주행 마블을 코스 안쪽으로 반사 |
| 회전 막대 | `0.68~0.80` | 라벤더 | 회전 운동량을 가진 옆방향 충돌 |

범퍼 탄성은 이전보다 약 30% 높다. 회전 막대도 기준 회전 속도를 높이고 경기마다 `0.86~1.14` 배율을 적용해 단순히 톡 튀는 접촉보다 분명한 횡방향 반사를 만든다. 탄성 벽은 일부 직선 구간에만 배치해 모든 벽이 과도하게 튀는 것을 막는다.

## 발견 3: 마지막 반전은 선택적 지연이어야 한다

이전 결승 구조는 수축부 양쪽의 점프 범퍼가 마블을 위로 되돌렸다. 선두뿐 아니라 뒤따른 마블도 같은 위치에 모여, 사실상 결승 앞에서 새로운 단판 경기를 시작하는 효과가 생겼다.

현재 결승 시퀀스:

`양측 중앙 유도 디플렉터 → 한쪽 위험 범퍼 → 같은 쪽 벽 방향 회전 막대 → 완만한 수축부 → 내부 폭 60px 통로 → 결승선`

- 위험 범퍼는 한쪽 진입선에만 있어 접촉하지 않은 마블의 진행을 방해하지 않는다.
- 회전 막대는 같은 쪽 벽을 향해 돌며 위험 경로를 고른 마블에게 두 번째 변수만 제공한다.
- 중앙·반대쪽에는 회전 각도와 무관하게 마블 3개 이상이 지나는 우회 폭을 항상 유지한다.
- 능동 장애물 뒤 수축부는 외벽 곡선만 사용해 추가 정체 없이 60px 결승 통로로 합류한다.
- 접촉 마블을 위로 되쏘거나 이전 구간으로 강제 복귀시키지 않는다.

최종 회전 막대는 폭 244px이고 위험 경로 점유율 상한은 60%다. 실제 위험 점유율은 57.83%이며, 회전 각도와 무관하게 159.59px, 마블 5.32개 폭의 우회로가 열린다. 위험 범퍼의 `x=365`, `105×30px`, 각도 `0.18rad`와 회전 속도 `-0.023`은 유지했다.

따라서 선두가 위험 범퍼나 회전 막대에 우연히 걸린 짧은 순간에 바로 뒤 마블이 열린 길로 내려가는 반전은 가능하지만, 모든 마블이 모일 때까지 결승을 닫지는 않는다.

## 발견 4: 결승 공정성은 순위 보존 지표로 측정해야 한다

같은 고정 24경기 표본에서 이전 양측 점프 범퍼 구조와 1.2.0을 비교했다.

| 지표 | 이전 구조 | 1.2.0 | 해석 |
|---|---:|---:|---|
| 결승 진입 선두 우승률 | 20.8% | 75.00% | 목표한 순위 보존·반전 균형 |
| 우승자의 결승 진입 평균 순위 | 3.25위 | 1.2917위 | 후미 무작위 당첨 감소 |
| 진입·최종 순위 Spearman 상관 | 0.366 | 0.79558 | 진입 대열 보존 |
| 결승 진입부터 첫 완주 | 4.84초 | 3.5847초 | 결승 앞 재정체 감소 |

결승 진입 선두가 패한 비율은 25.00%다. 마지막 반전이 눈에 보일 만큼 남지만, 결승 진입·최종 순위의 상관과 상위권 보존이 함께 유지돼 전체 레이스를 무효화하는 병목은 아니다. 진입 상위 3명의 최종 상위 3명 유지율은 81.94%다.

확장 72경기(5명·10명 × 시드 0~35)에서도 결승 진입 선두가 56회 우승해 전체 77.78%, 5명 75.00%, 10명 80.56%를 기록했다. Spearman 상관은 0.8598, 상위 3명 유지율은 86.57%, 진입 후 첫 완주는 3.358초이며 시간초과는 0건이다. 표준 24경기 홀드아웃만 분리하면 선두 우승률은 정확히 75.00%이므로 특정 소표본에만 맞춘 병목 조정으로 보지 않는다.

## 발견 5: 충돌 빈도와 선두 교체는 별도로 봐야 한다

고정 24경기(5명·10명 각 12시드)의 1.2.0 결과:

| 지표 | 결과 |
|---|---:|
| 평균 첫 완주 | 29.3792초 |
| 전체 장애물 접촉 참가자 비율 | 93.33% |
| 범퍼 접촉 참가자 비율 | 82.22% |
| 결승 위험 범퍼 접촉 참가자 비율 | 8.33% |
| 회전 막대 접촉 참가자 비율 | 68.89% |
| 결승 회전 막대 접촉 참가자 비율 | 5.56% |
| 전체 접촉 에피소드 | 경기당 21.5417회 |
| 결승 위험 범퍼 접촉 에피소드 | 경기당 1.1667회 |
| 결승 회전 막대 접촉 에피소드 | 경기당 0.5417회 |
| 0.5초 확인 선두 교체 | 경기당 6.1667회 |

범퍼 접촉률 82.22%는 경기 전체에서 반발 경험이 충분히 보인다는 뜻이지만, 결승 위험 범퍼와 결승 회전 막대의 참가자 접촉률은 각각 8.33%, 5.56%다. 즉 결승 장치는 전원 통과 관문이 아니라 선택적 사건으로 작동한다. 우승자의 결승 구간 평균 최대 역주행도 53.8266px로, 마블을 이전 구간까지 되쏘는 구조가 아니다.

구간별 확인 선두 교체:

| 구간 | 경기당 확인 선두 교체 |
|---|---:|
| 0–25% PIN DROP | 2.375 |
| 25–50% BUMPER RUN | 1.3333 |
| 50–75% SQUEEZE GATES | 1.125 |
| 75–100% FINAL MIX | 1.3333 |

후반으로 갈수록 대열이 정리되지만 마지막 구간에서도 실제 선두 교체가 남는다. 특정 구간에 최소 역전 횟수를 강제하지 않으며, 관측값만 회귀 지표로 사용한다.

## 발견 6: 추격·횡풍·힘장은 물리 경기와 양립하지 않는다

이전 추격 가속은 참가자 신원을 읽지 않았지만 선두와의 물리적 거리를 읽어 후미 마블에 하향 힘을 더했다. 횡풍과 공간 힘장도 모든 참가자에게 공통이었지만 화면에 보이지 않는 힘이었다.

1.2.0에서는 다음을 제거했다.

- 선두 거리 기반 추격 가속
- 시간대별 횡풍 펄스
- 위치 기반 혼합 힘장
- 결승 범퍼 접촉 뒤 속도 덮어쓰기
- 순위나 결과를 읽는 모든 힘

장시간 정체 안전장치는 40초 이후의 경기장 전체 중력 상승만 남긴다. 이 값은 같은 시각의 모든 미완주 마블에 동일하게 적용되며, 첫 완주 목표 30초보다 늦게 시작하므로 정상 경기의 선두 경쟁에 개입하지 않는다.

## 발견 7: 장애물 색은 외형보다 성격을 먼저 설명해야 한다

공용 5색을 장애물 순번대로 순환시키면 같은 색이 서로 다른 움직임을 뜻하게 된다. 1.2.0은 색을 역할에 고정했다.

- 핫핑크: 강하게 튕기는 범퍼
- 레몬: 작은 방향 분기를 만드는 핀
- 민트: 중앙으로 흐르게 하는 가이드와 벽 디플렉터
- 스카이: 범퍼급 탄성을 가진 벽
- 라벤더: 움직이는 회전 막대

라이트·다크 맵은 배경, 트랙, 벽, 글자, HUD 토큰만 바꾼다. 장애물 의미 색과 참가자 테마는 두 모드에서 유지되며 기본값은 라이트다.

핀·가이드·탄성 벽·범퍼·회전 막대는 테마별 외곽선을 함께 사용한다. 의미 색을 더 어둡거나 밝게 바꾸지 않고 형상 경계를 강화하므로 라이트·다크 전환 뒤에도 같은 색이 같은 장애물 역할을 뜻한다.

## 발견 8: 연출은 안정화된 물리 사건에서만 파생해야 한다

순간 순위는 충돌 해결 과정에서 프레임마다 바뀔 수 있다. 그대로 배너와 카메라에 연결하면 실제 추월보다 화면 떨림이 먼저 보인다.

현재 조건:

- 지속 선두 교체: 새 선두가 6개 캡처 프레임 동안 유지되고 한 마블 반지름 이상 앞섬. 다중 당첨에서는 이미 도착한 마블을 제외하고 `targetFinishCount`명 도착까지 미도착 경쟁자 사이의 교체를 계속 감지
- 접전: 첫 완주 전 선두와 2위의 간격이 마블 지름 2개 이하
- 결승 접근: 첫 완주 전 선두 진행률 88% 이상
- 포토 피니시: 1·2위 실제 도착 차이 0.35초 이하
- 속도 트레일: 연속 포즈의 실제 이동량이 임계값 이상
- 카메라 선두 전환: 새 선두가 0.5초 유지된 뒤 전환
- 같은 경기 다시 보기: 재생 에포크가 바뀌면 카메라 위치·속도·선두 후보와 일시 연출 초기화
- 모바일 순위: 첫 3행 밖의 현재 첫 미도착 경쟁자를 추가 표시
- 라이브 알림: 매 프레임 수치는 제외하고 안정화된 경기 사건만 전달

연출은 이미 계산된 프레임을 읽기만 하며 물리나 결과를 바꾸지 않는다.

결과 이력도 프레임 단위로 새 항목을 만들지 않는다. 결과 진입 즉시 같은 `runId`의 최소 기록을 저장하고, 실제 도착자 수가 증가할 때만 기존 항목을 갱신한다.

## 발견 9: 60px 통로와 전원 완주는 함께 검증해야 한다

결승 통로 자체의 내부 폭은 기존 요구대로 60px를 유지한다. 문제는 통로 폭보다 그 앞에서 마블을 위로 되돌리던 능동 장애물이었다. 한쪽 위험 경로와 열린 우회로, 무장애 수축부로 바꾼 뒤 고정 24경기 표본의 10명 경기에서 10명 전원이 실제 결승선을 통과했다.

자동 검사는 다음을 함께 확인해야 한다.

- 모든 독립 충돌체 사이 36px 최소 표면 간격
- 회전 막대 중심축 주변 마블 한 개 크기 안전 모델
- 각 경기용 직선 구간 양쪽의 중앙 유도 장애물
- 결승 위험 범퍼가 정확히 한 개인지
- 결승 회전 막대가 지정 벽 방향으로 도는지
- 위험 장애물 옆 영구 우회 폭이 마블 3개 이상인지
- 수축부에 대열 전체를 막는 장애물이 없는지
- 60px 결승 통로와 결승선 폭이 일치하는지

## 검증된 흐름

| 항목 | 결과 |
|---|---|
| 2~10명 명단 파싱 | 통과 |
| 11번째 이름 보존 및 시작 차단 | 통과 |
| 동일 이름 기본 차단·옵션 허용 | 통과 |
| 물리 전용 슬롯 배정의 시드 재현 | 통과 |
| 시뮬레이션의 슬롯 매핑 소유·계획 검증 | 통과 |
| 결과 선확정 옵션 부재 | 통과 |
| 120Hz 서브스텝·경계 위치/속도 보정 부재 | 통과 |
| 같은 경기 다시 보기 | 같은 우승자·순위, 초기화된 카메라 |
| 라이트 기본·다크 선택 | 동일 장애물 의미 색 유지 |
| 결과 기록 | 브라우저 로컬 저장, 최대 20개 |
| 고정 24경기 | 평균 첫 완주 29.3792초, 진입 선두 우승 75.00% |
| 확장 72경기 | 선두 우승 77.78%, 시간초과 0건 |
| 경계 건전성 36경기 | 시간초과·경계 6px 초과 이탈 0건 |
| 10명 표본 | 전원 실제 결승 통과 |
| 제품 자동 검증 | 53/53 통과 |

## 남은 관찰 항목

1. 표준 홀드아웃의 25.00% 결승 반전율이 장기 표본에서도 목표 범위에 머무는지 확인
2. 출발 슬롯별 우승률과 평균 결승 진입 순위의 장기 편향 집계
3. 탄성 벽 구간별 연속 재충돌이 저사양 브라우저 프레임 시간에 미치는 영향
4. 라이트·다크 모드에서 마블과 장애물 의미 색의 실제 방송 캡처 식별성
5. 효과음을 범퍼·탄성 벽·회전 막대·포토 피니시 물리 사건별로 분리

## 이전 설계 기록

1.1.4 이하의 양측 결승 점프 범퍼, 추격 가속, 횡풍·공간 힘장, 결과 선확정형은 역사적 구현이며 1.2.0의 현재 계약에는 포함되지 않는다. 세부 변경 이력은 [DEVELOPMENT_LOG.md](./DEVELOPMENT_LOG.md)를 참고한다.
