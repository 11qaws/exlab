# Ex Lab Race 시스템 명세 — 1.2.1

## 정적 충돌 재질과 재생 보간

- Matter.js 정적 Body 생성 직후 작성된 `friction`, `frictionStatic`, `restitution`을 모든 body part에 복원한다.
- 고정 유도 막대와 벽 부착 디플렉터는 무마찰 재질로 처리해 완만한 경사와 둥근 끝단에서 장시간 정지하지 않게 한다.
- 범퍼와 회전 막대의 시드 기반 반발계수는 실제 충돌 Body에 적용되어야 하며, 결과·순위 기반 추가 임펄스를 사용하지 않는다.
- 슬로모션에서도 도착·순위 판정은 정수 물리 프레임을 사용한다. 화면만 현재와 다음 물리 프레임 사이를 보간하며 다음 프레임의 도착 상태나 새 충돌 플래시는 미리 노출하지 않는다.
- 카메라 가속은 렌더 횟수가 아닌 소스 프레임 경과량을 사용한다.
- 고정 가이드 회귀 게이트는 8명·48시드에서 `이동량 < 0.5px/frame` 접촉이 30프레임 이상 이어지는 사례가 0건이어야 한다.

## 0. 물리 경기 계약

### 결과 결정

- Race는 물리 결과만 지원한다. 결과 선확정 모드, 결과 시드, 지정 우승자 입력은 존재하지 않는다.
- 경기 생성 단계에서 `createRaceSlotAssignment(candidates, raceSeed)`를 호출해 참가자와 익명 출발 슬롯을 한 번 배정한다.
- 참가자와 슬롯의 대응은 방송 대기 진입 전에 잠그며 경기 중 변경하지 않는다.
- `simulateRace`는 확정된 `slotToCandidateId`를 입력으로 받아 유효성을 검사하고, `RaceSimulation.slotToCandidateId`가 동일 매핑의 복사본을 결과까지 보유한다.
- `buildRacePlan`은 별도 매핑을 다시 만들지 않고 `RaceSimulation`이 보유한 매핑만 검증·사용한다.
- 시뮬레이션은 이름, 참가자 색, 당첨자, 예정 결과를 힘 계산에 사용하지 않는다. 실시간 거리와 미도착 순위는 아래에 명시한 제한형 추격 가속에만 사용한다.
- 실제 결승선 통과 순서만 `winnerIds`와 최종 순위를 결정한다.
- 같은 `raceSeed`와 `layoutSeed`는 같은 슬롯 대응, 경기 물성, 프레임, 결과를 재현한다.

### 시드 기반 불규칙 출발

- `layoutSeed`는 익명 슬롯별 x 지터를 결정한다. 각 지터의 범위는 `-12~+12px`이고 전체 평균은 정확히 0이어야 한다.
- 모든 마블의 시작 y좌표는 같으며 같은 물리 스텝에 동시에 해제한다.
- 시작 마블 사이의 거리는 최소 36px를 유지하고 모든 마블은 시작 구역 안에 있어야 한다.
- 배치 계산은 참가자 ID, 이름, 색, 당첨자, 예정 결과를 읽지 않는다.
- 같은 `layoutSeed`는 같은 슬롯별 시작 위치를 만들며, 필요하면 `createMarbleStartLayout`으로 동일 위치를 재계산한다. `RaceSimulation`에는 출발 묶음의 `layoutShift`와 해당 배치로 계산된 물리 프레임을 보존한다.

### 물리 응답

- 범퍼 접촉은 Matter.js가 계산한 충돌 법선과 경기별 반발계수만 사용한다.
- 범퍼 충돌 뒤 마블 속도를 직접 덮어쓰거나 추가 방향 충격을 주지 않는다.
- 외곽 경계를 벗어난 마블의 위치나 속도를 강제로 안쪽으로 되돌리는 보정도 사용하지 않는다.
- 한 60Hz 경기 스텝을 두 번의 120Hz 물리 서브스텝으로 계산해 빠른 마블과 고탄성 충돌의 벽 터널링을 Matter 충돌 단계에서 방지한다.
- 막대형 범퍼 반발계수는 경기별 `1.22~1.32`다. 이전 값보다 약 30% 높여 마블끼리 다시 충돌하는 횡방향 분산을 만든다.
- 회전 막대 반발계수는 `0.68~0.80`이며, 각 막대의 기준 회전 속도에 시드별 `0.86~1.14` 배율을 적용한다.
- 탄성 벽은 해당 구간의 외벽 전체에 범퍼와 같은 반발계수를 적용한다. 일반 벽과 가이드 레일은 표준 장애물 반발계수를 사용한다.
- 횡풍, 공간 힘장, 결과 기반 힘, 참가자별 지정 힘은 사용하지 않는다. 1.2.1의 제한형 추격 가속만 명시적 예외다.
- 40초까지 중력은 경기 시작 시 정한 값으로 유지한다. 40초 이후의 시간초과 방지 중력 상승은 모든 미완주 마블에 같은 시각·같은 비율로 적용하며 참가자·순위·결과를 읽지 않는다.

### 제한형 추격 가속

- 추격 가속은 아직 결승선을 통과하지 않은 후미 마블에만 +y 방향 힘으로 적용한다. 선두에는 추가 힘을 주지 않는다.
- 현재 미도착 선두와의 세로 격차가 600px 이하이면 추격력은 0이다. 600~1,000px에서는 부드러운 램프로 증가하고 1,000px부터 거리 계수는 상한에 도달한다.
- 1,000px 이상에서 실시간 2위는 기본 중력의 25%, 최하위는 32%를 최대 추가 힘으로 받는다. 중간 순위는 두 값 사이를 연속적으로 보간한다.
- 후미 마블이 선두를 따라잡는 세로 상대 속도가 커질수록 힘을 감쇠하고, closing speed가 600px/s 이상이면 추격력은 0이다.
- 선두 감속, 마블 위치·속도 덮어쓰기, 완주자 대상 힘은 허용하지 않는다.
- 추격 계산은 당첨자, 예정 결과, 참가자 ID·이름·색을 참조하지 않는다. 같은 시드와 같은 물리 상태는 같은 힘과 결과를 만든다.

### 결승 공정성

- 결승 시퀀스는 `양측 중앙 유도 디플렉터 → 한쪽 위험 범퍼 → 같은 쪽 벽을 향해 도는 회전 막대 → 완만한 수축부 → 내부 폭 60px 통로 → 결승선`이다.
- 위험 범퍼와 회전 막대는 한쪽 경로에만 놓는다. 모든 회전 각도에서 중앙·반대쪽에 최소 마블 3개 폭의 우회로를 영구 보장한다.
- 최종 회전 막대 폭은 244px이고 위험 경로 점유율 상한은 60%다. 현재 형상의 실제 위험 점유율은 57.83%, 상시 우회 폭은 159.59px로 마블 지름의 5.32배다.
- 위험 경로를 탄 마블은 접촉 법선에 따라 잠시 지연될 수 있지만, 위쪽으로 강제 발사하거나 결승 진입 전 위치로 되돌리지 않는다.
- 수축부에는 능동 장애물을 두지 않고 경계 곡선만으로 60px 통로에 합류시킨다.
- 결승 직전의 반전은 “모두를 붙잡아 재출발”시키는 방식이 아니라, 우연히 위험 장애물에 걸린 선두를 열린 우회로의 후속 마블이 추월하는 방식이어야 한다.
- 자동 검사는 위험 장애물 개수·위치, 회전 방향, 영구 우회 폭, 수축부의 비폐색, 60px 결승 통로를 함께 검증한다.

### 장애물 의미 색상과 맵 테마

| 장애물 역할 | 공용 색 | 값 |
|---|---|---|
| 범퍼 | 핫핑크 | `#ffb6c1` |
| 핀 | 레몬 | `#ffd166` |
| 가이드·디플렉터 | 민트 | `#34e0a8` |
| 탄성 벽 | 스카이 | `#4ea9f0` |
| 회전 막대 | 라벤더 | `#7e57c2` |

- 위 5색은 공용 스트리머 팔레트의 고정 순서이며 장식이 아니라 장애물 성격을 설명한다.
- 기본 맵 모드는 `light`다. 사용자는 `dark`를 선택할 수 있다.
- 코스 배경, 트랙, 벽, 텍스트, HUD 색은 모드별 토큰을 사용한다.
- 장애물 의미 색과 참가자 마블 테마는 맵 모드가 바뀌어도 유지한다.
- 핀·가이드·탄성 벽·범퍼·회전 막대에는 라이트·다크 테마별 외곽선을 적용해 같은 5색을 유지하면서 트랙과의 형상 경계를 강화한다.

### 물리 기반 경기 연출

- `3 → 2 → 1 → GO` 동안 물리 프레임은 진행하지 않는다.
- 선두 교체는 새 선두가 6개 캡처 프레임 동안 유지되고 한 마블 반지름 이상 앞선 경우에만 표시한다.
- 다중 당첨 경기에서는 이미 도착한 마블을 경쟁 선두에서 제외하고, `resultGateCount`명이 도착할 때까지 미도착 경쟁자 사이의 선두 교체를 계속 감지한다.
- 선두와 2위의 간격이 마블 지름 2개 이하면 접전 상태를 표시한다.
- 선두가 코스 88%를 통과하면 결승 접근 상태를 표시한다.
- 1·2위 도착 차이가 0.35초 이하면 포토 피니시를 표시한다.
- 속도 트레일은 연속 프레임의 실제 이동량에서만 생성한다.
- 같은 경기 다시 보기에는 재생 에포크를 증가시켜 카메라의 위치·속도·선두 후보와 일시 연출을 초기화한다.
- 프레임마다 바뀌는 진행률·거리·시간 상태에는 `aria-live`를 사용하지 않는다. 지속 선두 교체·접전·결승 접근·포토 피니시처럼 안정화된 사건만 라이브 알림에 보낸다.

### FINAL MIX 역전 시네마틱

- 코스 진행률 75~100%인 `FINAL MIX`에서 첫 완주 전 전체 1·2위 사이의 선두 교체가 6개 캡처 프레임 동안 유지되고 15px 이상 앞선 경우만 대상으로 한다.
- 첫 완주자가 나온 뒤 미도착 마블 사이에서 바뀌는 잔여 순위에는 슬로모션과 클로즈업을 적용하지 않는다.
- 시네마틱은 확인 완료 프레임이 아니라 실제 선두 교체가 시작된 `holdStartFrameIndex`의 0.3초 전부터 프리롤한다.
- 한 역전 이벤트는 같은 재생 에포크에서 한 번만 재생한다.
- `holdStartFrameIndex` 전 0.3초와 후 0.2초, 총 0.5초 동안 프레임 재생 속도를 0.5배로 낮추고 카메라를 100% 확대해 최대 2.0배로 클로즈업한다.
- 시네마틱 도중 새 확인 역전이 겹치면 최신 이벤트의 `holdStartFrameIndex`를 기준으로 대상과 0.5초 지속 시간을 갱신한다.
- `prefers-reduced-motion`에서는 정상 재생 속도와 무확대를 유지하고 정적 역전 큐만 표시한다.
- 시네마틱은 이미 계산된 물리 프레임의 표시 속도와 카메라만 바꾸며 프레임 내용, 충돌, 완주 순서, 당첨 결과를 바꾸지 않는다.

### 1.2.1 검증 기준

- 공정성·격차 벤치마크는 5명·10명 각 12시드, 총 24경기를 사용한다.
- 평균 첫 도착은 `31.0972초`다.
- 결승 진입 선두 우승률은 전체 `83.333%`, 5명 `83.333%`, 10명 `83.333%`다.
- 결승 진입 상위 3명 유지율은 `0.83333`, 진입·최종 순위 Spearman 상관은 `0.73838`, 15프레임 카메라 지연 기준 확인 선두 교체는 경기당 `7.2917회`다.
- 선두와의 평균 격차는 `525.06px`, 경기별 p95 격차 평균은 `1,386.78px`, 경기별 최대 격차 평균은 `1,707.09px`, 절대 최대 격차는 `3,073.69px`다.
- 선두 격차가 1,000px을 넘는 프레임 비율은 `14.888%`다.
- 확장 72경기에서는 평균 첫 도착 `30.5241초`, 결승 진입 선두 우승률 `73.611%`, 확인 선두 교체 `7.6667회/경기`로 약 75% 목표를 재확인한다.
- `7.2917회`는 FINAL MIX 시네마틱의 6프레임·15px 역전 이벤트 수와 같은 지표가 아니다.
- 출발 위치 공정성 screening은 `npm run benchmark:positions`로 5명·10명만 각 120경기 실행한다. 슬롯은 매 경기 실제 시작 x좌표의 왼쪽부터 순서대로 정의한다.
- 재질 복원 후 5명 120경기 우승 수는 `14 / 31 / 23 / 30 / 22`, 균등성·선형 x·가장자리·결합 raw p는 `0.0947 / 0.3559 / 0.0532 / 0.1005`다.
- 재질 복원 후 10명 120경기 우승 수는 `14 / 13 / 9 / 15 / 9 / 13 / 18 / 12 / 7 / 10`, raw p는 `0.5174 / 0.4184 / 0.5331 / 0.5934`다.
- 균등성, 실제 x의 선형 항, 가장자리의 이차 항, 선형·이차 결합 검정을 5명·10명에 각각 적용한 8개 가설은 Holm 보정 뒤 모두 유의하지 않았다.
- 가장 작은 raw p는 5명 가장자리 효과 `0.0532`이고 Holm 보정 p는 `0.4258`이다. 비유의 결과는 위치별 승률이 완전히 동일하다는 증명이 아니다.
- 기본 120경기는 기대도수 12 이상을 확보하는 screening이다. 정식 release gate는 5명·10명 각각 900경기를 권장한다.
- 밸런스·공정성 실험 대상은 5명 이상이다. 2~4명 경기는 기능 호환성만 보장하며 추격 계수와 공정성 수치의 적용 대상에서 제외한다.

### 1.2.0 물리 검증 기준 — 역사 기준선

- 공정성 벤치마크는 5명·10명 각 12시드, 총 24경기를 사용한다.
- 평균 첫 완주 `29.3792초`, 결승 진입 선두 우승률 `75.00%`, 우승자 진입 평균 순위 `1.2917위`, 진입 상위 3명 유지율 `81.94%`, 진입·최종 순위 Spearman 상관 `0.79558`, 진입 후 첫 완주 `3.5847초`를 기준값으로 기록한다.
- 우승자의 결승 구간 평균 최대 역주행은 `53.8266px`다.
- 전체 장애물·범퍼·결승 위험 범퍼·회전 막대·결승 회전 막대 접촉 참가자 비율은 각각 `93.33% / 82.22% / 8.33% / 68.89% / 5.56%`다.
- 전체 접촉, 결승 위험 범퍼, 결승 회전 막대 접촉 에피소드는 경기당 각각 `21.5417 / 1.1667 / 0.5417회`다.
- 확인 선두 교체는 경기당 `6.1667회`, 네 구간에서 `2.375 / 1.3333 / 1.125 / 1.3333회`다.
- 별도 물리 건전성 검증은 2·5·10명 각 12시드, 총 36경기를 사용한다. 시간초과와 경계 6px 초과 이탈은 모두 0건이어야 한다.
- 확장 공정성 검증은 5명·10명 각 36시드, 총 72경기를 사용한다. 결승 진입 선두 우승률은 전체 `77.78%`(56/72), 5명 `75.00%`, 10명 `80.56%`이며 Spearman 상관 `0.8598`, 상위 3명 유지율 `86.57%`, 진입 후 첫 완주 `3.358초`, 시간초과 0건을 기준으로 한다.
- 표준 24경기 홀드아웃의 결승 진입 선두 우승률은 정확히 `75.00%`여야 한다.
- 1.2.0은 실시간 순위·선두 거리 기반 힘을 사용하지 않은 순수 물리 기준선이다.
- 1.2.0의 `layoutSeed`는 개별 슬롯 지터가 아니라 출발 묶음 전체의 제한된 좌우 이동을 결정했다.
- 1.2.0 결과 화면은 N번째 당첨자 도착 뒤 1.2초 확인 연출 후 열렸다.

## 1. 제품 계층과 명칭

- 전체 게임 프로젝트: `Ex Lab`
- 게임: `Roulette`, `Race`
- 현재 독립 구현 범위: `Ex Lab / Race`
- 공용 셸은 프로젝트명과 게임 전환을 담당하고, 게임 내부 화면은 게임명만 사용한다.
- 기존 특정 서비스명은 UI, 메타데이터, 저장 키의 신규 명칭에서 사용하지 않는다.

## 2. 타이포그래피

- 본문, 입력, 버튼, 순위표: `Pretendard` 우선
- 프로젝트명, 게임명, 주요 제목, 카운트다운: Race의 무거운 쇼다운형 표시 서체 토큰
- 서체는 컴포넌트에서 직접 지정하지 않고 `--font-body`, `--font-display` 토큰을 사용한다.

## 3. 참가자와 인원별 테마

### 참가자 계약

```ts
type Candidate = {
  id: string;
  name: string;
  number: number;
  theme: ParticipantTheme;
};
```

`ParticipantTheme`은 `primary`, `onPrimary`, `surface`, `onSurface`, `border`를 가진다. 준비 명단, 코스 미리보기, 경기 공, 실시간 순위표, 당첨자, 최종 순위가 모두 같은 후보자의 같은 테마를 사용한다.

### 대비 규칙

- 기본 본문 대비: 4.5:1 이상
- 작은 텍스트와 상태 표식: 6.5:1 이상
- 색 자체의 밝기를 맞추는 대신 각 색 위의 텍스트 대비를 맞춘다.
- 10개 참가자 테마는 조 안에서 중복되지 않으며, 다음 조에서 같은 슬롯 체계를 재사용할 수 있다.
- 장애물 의미 색은 참가자 식별 색과 별도 레이어로 관리한다.

### 동일 이름

- 기본값: 미허용
- 동일 이름이 있으면 명단은 보존하지만 유효하지 않은 상태로 표시하고 방송 화면 진입을 막는다.
- 운영자가 `동일 이름 허용`을 켜면 서로 다른 내부 ID와 번호로 참가한다.

## 4. 전체 명단과 조 편성

- 전체 명단을 먼저 확정한 뒤 경기 조를 만든다.
- 한 조는 최소 2명, 최대 10명이다.
- 기본 조 수는 1조다.
- 전체 인원이 10명을 넘으면 최소 조 수를 `ceil(전체 인원 / 10)`으로 자동 보정한다.
- 운영자는 `floor(전체 인원 / 2)` 범위까지 조 수를 늘려 더 작은 조로 나눌 수 있다.
- 각 조의 인원 차이는 최대 1명이어야 한다.
- 예: 32명은 기본 4조이며 `8명 × 4조`로 편성한다. 운영자는 8조를 선택해 `4명 × 8조`로 줄일 수 있다.
- 준비 화면은 전체 인원, 조 수, 각 조 인원, 현재 방송에 올릴 조를 동시에 보여준다.

## 5. 방송 시작 상태

```text
ready
  → generating
  → waiting
  → countdown(3, 2, 1, GO)
  → running
  → result-delay
  → result
```

- `방송 화면 열기`: 명단, 조, 당첨 인원, 물리 시드, 슬롯 대응을 잠그고 `waiting`으로 이동한다.
- `waiting`: 첫 프레임과 명단만 보인다. 카운트다운과 물리 프레임은 진행하지 않는다.
- `경기 시작`: 운영자가 방송 화면에서 눌러야만 카운트다운이 시작된다.
- `설정으로 돌아가기`: 대기 중인 계획을 폐기하고 준비 화면으로 돌아간다.
- `같은 경기 다시 보기`: 이미 잠근 프레임과 결과를 사용하되 카메라·연출 상태는 새 재생 에포크로 초기화한다.

## 6. N명 당첨 경기

- 조별 `winnerCount`는 1명 이상 조 인원 이하다.
- `podiumCount`는 `min(3, 참가자 수)`다. 2명 경기는 전원, 3명 이상 경기는 최소 3위까지 화면에서 실제 도착해야 한다.
- `resultGateCount`는 `max(winnerCount, podiumCount)`다. 당첨 인원과 포디엄 인원 중 더 큰 기준까지 물리 시뮬레이션을 준비한다.
- 첫 완주자나 N번째 당첨자가 나와도 `resultGateCount`와 3초 조건을 충족하기 전에는 결과 화면으로 전환하지 않는다.
- 실시간 화면은 `확정된 당첨자 수 / 목표 당첨자 수`를 표시한다.
- N번째 공이 실제 결승선을 통과한 프레임을 `awardFrameIndex`로 기록한다.
- `resultGateCount`번째 도착이 실제 화면 재생에 나타난 시점을 결과 홀드 시작점으로 기록한다.
- 결과 화면은 결과 게이트 도착 이후 사용자가 실제로 볼 수 있는 경기 화면을 3,000ms 유지한 뒤 연다. 숨김 탭에서는 이 시간이 진행되지 않는다.
- `result-delay`에서도 물리 프레임과 순위표는 계속 진행한다.
- 결과 화면은 실제 결승 통과로 확정된 `winnerIds` 전체를 순위와 함께 표시한다.
- 전체 순위표는 실제 결승 통과자만 도착 순서대로 채운다. 미도착 행은 이름·테마·예상 순위를 노출하지 않고 공란으로 둔다.
- 이후 참가자가 결승선을 통과하면 현재 프레임의 `finishedSlotIds`를 기준으로 해당 순위 행을 실시간으로 채운다.
- 결과 진입 즉시 같은 `runId`의 최소 결과를 저장하고, `finishedSlotIds`의 수가 증가할 때만 기존 이력을 갱신한다. 같은 프레임이나 같은 도착자 수로 중복 기록을 만들지 않는다.
- 모바일 순위표는 기본 상위 3행과 별도로, 그 밖에 있는 현재 첫 미도착 경쟁자 한 명을 항상 표시한다.
- 제한 시간까지 도착하지 않은 행은 공란으로 남기며 진행 거리 기반 임시 순위를 결과표에 사용하지 않는다.
- 제한 시간 안에 `resultGateCount`명이 실제로 통과하지 못하면 추정 당첨자나 포디엄을 만들지 않고 경기 생성 오류로 처리한다.
- 1.2.0의 물리 건전성 36경기 표본에서는 2·5·10명 모든 참가자가 실제 결승선을 통과했다. 이 문장은 역사 기준선이며 1.2.1의 2~4명 추격 튜닝을 의미하지 않는다.

## 7. 풀 패키지 설계 개념

풀 패키지는 이번 독립 Race 화면 위에 놓이는 상위 오케스트레이터다.

```ts
type PackagePlan = {
  packageId: string;
  game: "roulette" | "race";
  roster: Candidate[];
  groups: CandidateGroup[];
  winnersPerGroup: number;
  currentGroupIndex: number;
  status: "draft" | "ready" | "running" | "complete";
};

type GroupResult = {
  packageId: string;
  groupId: string;
  winnerIds: string[];
  rankedCandidateIds: string[];
  completedAt: string;
};
```

### 순차 진행

1. 전체 명단과 조 편성을 잠근다.
2. 1조 방송 대기 화면을 연다.
3. 운영자가 경기를 시작한다.
4. N명 당첨이 확정되면 `GroupResult`를 저장한다.
5. `다음 조 준비`로 다음 조의 새 Race 계획을 만든다.
6. 마지막 조가 끝나면 모든 `GroupResult`를 합쳐 패키지 결과를 표시한다.

### 종합 결과

- 조별 당첨자와 조별 순위를 원본 그대로 보존한다.
- 전체 결과의 기본 정렬은 `조 순서 → 조 내 도착 순서`다.
- 조 간 기록 시간을 직접 비교해 전체 순위를 만들지 않는다. 서로 다른 시드와 물리 조건이므로 같은 척도로 볼 수 없기 때문이다.
- 추후 결승 라운드를 사용할 경우 조별 당첨자만 새 명단으로 모아 별도 `PackagePlan`을 만든다.
- 중단 후 재개를 위해 현재 조, 완료된 조 결과, 잠긴 명단과 옵션을 함께 저장한다.

## 8. Roulette와의 통합 포인트

- 공용: Ex Lab 셸, Pretendard, 참가자 명단 편집기, 동일 이름 정책, 참가자 테마, 방송 대기 상태, 결과 저장 계약
- Roulette 전용: 휠 미리보기, 회전 연출, 1회 추첨 계약
- Race 전용: 조 편성, 물리 코스, 장애물 의미 색, N명 결승 통과 계약
- 공용 어댑터는 `RunRequest → GamePlan → GroupResult` 경계를 사용하고 게임 내부 물리 타입을 노출하지 않는다.
