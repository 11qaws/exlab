# Development Log

## 2026-07-27 1.3.3 인게임 결과 UI 통합

- Roulette와 Showdown이 함께 사용하는 결과 프레젠테이션 상태를 `live → evidence → hero → docking → settled`로 명문화하고, `runId + presentationId` 토큰으로 지난 경기의 지연 타이머와 콜백이 새 화면을 덮지 못하게 했다.
- 결과 화면에 필요한 우승자·순위·요약·무대 좌표를 불변 projection으로 고정해 게임 판정과 화면 연출을 분리했다. 결과를 다시 보여줄 때도 새 presentation ID를 사용한다.
- Showdown은 경기 종료 뒤 별도 결과 페이지로 교체하지 않는다. 같은 `RaceCanvas`와 카메라·코스·마블을 유지한 채 결승선 위 우승 Hero가 나타나고, 오른쪽 라이브 순위표가 도착 결과 레일로 전환된다.
- Showdown 결과 레일은 3위 발표 뒤에도 실제 도착 순서와 골인 시각을 계속 받아 갱신하며, 미도착 순위는 공란으로 둔다. 결과 동작 버튼과 실행 정보는 Hero 도킹이 끝난 뒤에만 표시한다.
- Roulette 우승 Hero를 룰렛이 있던 방송 무대 안에 유지하고, 원판의 결과 근거 위에서 발표한 뒤 누적 결과 레일의 최신 행으로 400ms 동안 도킹하도록 연결했다. Hero가 도킹되는 동안에도 동일 컴포넌트를 유지해 별도 결과 창으로 보이는 단절을 없앴다.
- 두 게임의 진입·도킹은 280~400ms 범위로 통일했고, `prefers-reduced-motion`에서는 이동과 확대 없이 100ms 페이드만 사용한다. 기존 물리 판정, 룰렛 결과 커밋, Showdown 결승 통과 순서는 변경하지 않았다.
- 1600×900, 1024×768, 390×844 실제 브라우저에서 Showdown의 동일 경기장 유지와 도착 결과 갱신, Roulette의 `motion → hero → docking → settled`, 반응형 폭과 가로 넘침 부재를 확인했다. 브라우저 경고·오류는 없었다.
- 생산 빌드, TypeScript, ESLint, diff whitespace 검사와 117개 자동 테스트를 통과했으며 이 변경 묶음을 1.3.3 비공개 배포 기준선으로 확정했다.

## 2026-07-27 1.3.2 Showdown 순위표·설계 화면 안정화

- `@scope (.showdown-game)`의 루트에 일반 `.race-screen` 선택자가 매칭되지 않아 라이트 맵에서도 밝은 순위 글자가 라이트 셸 위에 표시되던 문제를 `:scope.race-screen` 계열 루트 선택자로 수정했다.
- 통합 셸 안의 경기·결과 화면 높이를 공통 헤더를 뺀 실제 뷰포트로 제한해 방송 화면에 불필요한 페이지 스크롤이 생기지 않게 했다.
- 데스크톱 Showdown 준비 화면을 한 뷰포트 안에 고정하고, 긴 설정은 왼쪽 설정 패널 안에서만 스크롤되도록 변경했다. 미리보기와 실행 버튼은 화면 안에 계속 유지된다.
- 최근 경기 기록을 별도 하단 영역에서 `경기 방식` 세부 설정 안으로 옮겨 기록이 있을 때도 설계 화면 전체가 아래로 늘어나지 않게 했다.
- 라이트 맵 순위표의 실측 대비는 17.3:1이며, 1600×900·1280×720·1024×768에서 문서 높이가 뷰포트와 일치하고 가로 넘침이 없음을 브라우저에서 확인했다. 900px 이하에서는 기존 반응형 세로 스크롤을 유지한다.
- 생산 빌드, TypeScript, ESLint, diff whitespace 검사와 108개 자동 테스트를 통과했으며 이 변경 묶음을 1.3.2 비공개 배포 기준선으로 확정했다.

## 2026-07-27 1.3.1 exlab 통합 안정화·공통 테마·명칭 이전

- Roulette와 Showdown 준비 화면을 같은 `SetupWorkspace` 정보 구조로 통일했다. 공통 명단·동일 이름 정책·스트리머 테마는 한 곳에서 편집하고, 게임별 설정·미리보기·세부 설정·실행 버튼만 각 게임이 제공한다.
- 공통 명단은 인원 제한 없이 보존하고 Showdown 진입 시 조당 최대 10명으로 균형 편성한다. 32명은 기본 4조이며 더 많은 조로 세분화할 수 있다.
- 게임 surface를 한 번 방문하면 keep-alive로 유지해 게임을 오가도 제목·조 편성·연출 설정 초안이 사라지지 않게 했다. 진행 중이거나 명단 편집 중에는 게임 전환과 공통 설정 변경을 잠근다.
- `exclipper/rettohighlight`의 프로필 artifact를 바탕으로 아모레또·유레카·세나 아르벨·토로리 코코·망징이 5개 스트리머 카드를 구성했다. 선택 테마는 공통 셸과 Roulette·Showdown의 제품 chrome에 즉시 적용하되 참가자 구분색, 장애물 역할색, Showdown 물리와 결과에는 관여하지 않는다.
- 전체 타이포그래피를 `Inter`, `Pretendard`, 시스템 산세리프 폴백으로 통일하고 serif 계열을 제거했다.
- 통합 과정에서 깨진 Roulette 준비 화면의 이중 명단 편집, 빈 설정 공간, 공통 헤더 높이를 반영하지 않던 라이브 뷰포트, 비활성 탭의 스크롤 이동을 복구했다. 방송 대기·고속 회전·32명 긴 이름 환경에서도 데스크톱 화면에 가로 넘침이 없음을 확인했다.
- 공통 저장소의 정본을 `exlab:*`로 이전하고 기존 `ex-lab:*`, `retto-roulette-*`, `marble-game:roster` 키를 검증된 fallback과 rollback mirror로만 유지했다. Roulette pending 결과·상품 배정·기록과 Showdown 기록도 같은 방식으로 무손실 이전한다.
- 사용자 노출명, 패키지명, 앱 adapter, Showdown surface, Roulette 스타일 파일과 시스템 명세를 lowercase `exlab` 기준으로 바꿨다. 기존 Sites 프로젝트 ID와 배포 origin은 로컬 저장소 호환을 위해 유지한다.
- `exlab`과 `Roulette · Showdown`만 표시하는 새 1.91:1 OG 이미지를 생성해 `public/og.png`에 반영했다.
- 명단 대화상자는 열릴 때 입력란에 초점을 주고 Tab 순환과 Escape 닫기를 지원하며, 닫힌 뒤 원래의 `명단 편집` 버튼으로 초점을 복구한다.
- 생산 빌드, TypeScript, ESLint, diff whitespace 검사와 106개 자동 테스트를 통과했다. 32명의 긴 한국어 이름으로 데스크톱·태블릿·모바일 준비 화면, 게임 전환 상태 보존, 4조 자동 편성, 테마 저장, Roulette 방송 화면을 브라우저에서 확인했다.

## 2026-07-27 1.3.0 Ex Lab Roulette·Showdown 통합

- Showdown 단독 진입 화면을 Ex Lab 공통 셸로 교체하고, 게임 레지스트리에서 `Roulette`와 `Showdown`을 선택하는 실용형 운영 구조로 통합했다.
- 패키지 이름도 Race 전용 `ex-lab-race`에서 전체 프로젝트 이름인 `ex-lab`으로 정리했다.
- 공통 셸은 공유 명단, 마지막으로 사용한 게임과 실행 중 게임 전환 잠금만 소유한다. 셸은 고정 라이트 UI이며 지원이 부분적인 전역 라이트·다크·시스템 테마 선택과 저장은 제공하지 않는다. Showdown 코스의 기본 라이트·선택형 다크 맵 토글은 게임 전용 상태로 유지한다.
- 첫 진입에는 마지막 선택 게임만 `React.lazy`로 불러오고, 실제로 방문한 Roulette·Showdown surface만 keep-alive로 유지한다. 게임을 오가도 각 설정 초안은 보존되며 비활성 adapter의 `active=false`가 숨은 미리보기·타이머·애니메이션을 멈춘다.
- 공통 헤더에 현재 유효 인원을 표시하는 `참가자 N명` 명단 대화상자를 추가했다. 대화상자의 한 줄 한 명 입력과 동일 이름 정책을 두 게임이 함께 사용하며, 진행 중에는 편집을 잠근다.
- 동일 이름 기본값은 미허용이다. 허용하면 중복 이름을 dedupe하지 않고 입력된 각 occurrence를 서로 다른 참가 번호로 보존하며, 정책은 `ex-lab:allow-duplicate-names:v1`에 저장한다.
- 공유 명단의 단일 키를 `ex-lab:roster:v1`로 정했다. 새 키가 없을 때 Showdown의 Race 레거시 저장 키 `marble-game:roster`를 한 번 가져오며, 이후 공통 명단 저장은 레거시 키에도 mirror해 이전 버전 롤백 호환성을 유지한다. 별도 origin의 기존 Roulette 저장소는 자동 이전 범위에서 제외했다.
- catalog와 런타임 adapter 등록을 분리해 이후 게임도 공통 셸을 수정하지 않고 같은 입력·활동 상태 계약으로 추가할 수 있게 했다.
- Showdown 준비 화면의 큰 홍보형 제목과 장문 소개를 제거하고 게임 설정, 미리보기, 주 행동을 바로 볼 수 있게 했다. Showdown의 내부 Race 엔진과 Matter.js 물리, 결승 통과 결과, 라이브 시계와 골인 기록은 1.2.3 계약을 유지한다.
- Showdown CSS는 `.showdown-game`, Roulette CSS는 `.roulette-game`의 `@scope`로 격리해 게임 간 전역 선택자 유출을 막았다.
- 본문과 제목을 포함한 전체 타이포그래피를 산세리프로 통일하고 공통 셸의 하위 요소 override가 `Inter`, `Pretendard`, 시스템 산세리프 폴백을 강제한다. 제목 위계는 별도 장식 서체가 아니라 굵기와 자간으로 구분한다.
- Roulette의 원판·다트 좌표로 결과를 커밋하는 물리 결과 경계, 실행당 단일 결과 확정, 오래된 geometry 콜백 차단과 pending 결과 복구를 통합 adapter에서도 보존했다.
- Showdown 저장 이력은 JSON 배열과 필수 필드를 행별 검증해 손상된 레거시 행은 버리고, 유효한 구형 `winnerName`은 `winnerNames` 배열로 이관한 뒤 최대 20건만 복원한다.
- 상위 Roulette의 Vitest 설정에서 `Codex/workspace/**`를 제외해 하위 Node 테스트를 “No test suite”로 잘못 수집하던 중첩 작업공간 오염을 차단했다. Roulette 단독 기준선 `npx vitest run src`는 20개 파일·149개 테스트를 통과했다.
- Vitest에는 Jest의 `--runInBand` 옵션이 없으므로 해당 시도는 유효한 기준선 명령으로 기록하지 않는다.

## 2026-07-27 1.2.3 라이브 경기 시계·결과 발표 골인 기록

- 라이브 순위 제목에 현재 경기 시간을 `TIME MM:SS.cc`로 추가했다. 새 패널을 만들지 않고 기존 제목과 병합해 10명 순위표의 세로 공간을 유지한다.
- 현재 시간은 브라우저 경과 시간이 아니라 캔버스가 표현 중인 `renderFrameIndex`를 30fps 물리 시간으로 변환한다. FINAL MIX 슬로모션 중에도 화면 속 장면과 같은 속도로 진행한다.
- `resolveRaceElapsedMs`를 추가하고 슬롯별 `FinishRecord` 계산과 라이브 시계가 같은 프레임-밀리초 변환을 공유하도록 했다.
- 결과 발표의 각 당첨자 카드에 `골인 MM:SS.cc`를 표시하고, 실시간 도착 순위에도 도착자별 동일 기록을 유지한다. 미도착 결과 행은 기존 규칙대로 공란이다.
- 현재 경기 시간은 프레임마다 바뀌므로 라이브 영역으로 공지하지 않고 `<time aria-label="현재 경기 시간 …">`으로만 의미를 제공한다.
- 당첨자·도착 순위의 시간은 최초 결승 통과 프레임에서 고정되며 결과 전환 대기, 슬로모션 재생 시간, 브라우저 프레임 저하가 값을 바꾸지 않는다.

## 2026-07-26 1.2.2 라이브 순위 골인 시각·0.2초 카드 교체

- 라이브 순위의 마지막 열을 기존 `당첨` 텍스트에서 고정 폭 골인 시각으로 병합했다. 미도착은 `—`, 도착 뒤에는 `MM:SS.cc`를 표시해 순위 카드 폭이 변하지 않는다.
- 골인 시각은 브라우저 시계나 슬로모션 재생 시간이 아니라 `RaceFrame.finishedSlotIds`에 슬롯이 처음 등장한 30fps 물리 프레임으로 계산한다. 전체 기록은 한 번 색인하되 현재 프레임에서 실제로 도착한 슬롯에만 노출해 미래 결과를 미리 보여주지 않는다.
- 순위 행은 참가자 이름이 아닌 경기 내부 `slotId`를 React key로 사용한다. `getSnapshotBeforeUpdate`에서 현재 화면 좌표를 저장하고 갱신 뒤 새 좌표와의 차이를 200ms ease-out으로 보간하는 FLIP 전환을 추가했다.
- 연속 순위 교체 중에는 진행 중인 transform이 적용된 실제 화면 위치를 다음 시작점으로 사용한다. 데스크톱 세로 이동뿐 아니라 모바일 3열의 좌우 이동도 함께 계산한다.
- Web Animations API가 없을 때는 같은 200ms CSS transform 전환으로 대체한다. `prefers-reduced-motion`에서는 실행 중 애니메이션을 취소하고 즉시 새 순서를 표시한다.
- 모바일에서 `display:none`인 4위 이하 행의 0×0 좌표는 애니메이션에서 제외해 화면 원점에서 카드가 날아오는 현상을 방지한다.
- 10명의 긴 한국어 이름으로 1600×900, 1024×768, 390×844 화면을 확인했다. 가로 넘침은 0건이었고 실제 골인 시각 `00:36.10`, 다중 카드 transform, 모바일 3열 표시를 확인했다.

## 2026-07-26 1.2.1 고정 가이드 정지·슬로모션 프레임 보완

- Matter.js는 `isStatic: true` Body를 만들 때 요청한 `friction`과 `restitution`을 각각 `1`과 `0`으로 덮어쓴다. 정적 충돌체 생성 직후 모든 part에 작성된 재질을 다시 적용하는 공통 생성 경로를 추가했다.
- 고정 유도 막대와 벽 부착 디플렉터는 `friction=0`, `frictionStatic=0`으로 복원했다. 기존 둥근 끝단 형상은 유지하며, 결과나 순위를 보지 않고 중력과 충돌 법선만으로 끝단을 빠져나간다.
- 범퍼와 회전 막대도 같은 생성 경로를 사용해 각각의 시드 기반 반발계수가 실제 Matter Body에 적용되도록 수정했다.
- 수정 전 48개 시드·384개 마블 검사에서는 고정 선반 위 저속 접촉이 최장 76.1초 지속됐다. 수정 후 같은 표본에서 프레임당 이동 0.5px 미만이 1초 이상 이어진 사례는 0건이다.
- `npm run benchmark:stalls`를 추가해 8명·48시드의 고정 가이드 장기 체류를 재현 가능한 회귀 게이트로 관리한다.
- 30fps 물리 기록을 0.5배속으로 재생하면서 정수 프레임으로 내림해 실제 표시가 15fps가 되던 문제를 수정했다. 경기 판정은 기존 정수 프레임을 유지하고, 슬로모션 렌더만 인접한 물리 프레임의 마블 위치·각도·회전 막대를 소수 프레임으로 보간한다.
- 순위·도착 명단과 새 범퍼 충돌 플래시는 다음 정수 프레임 전까지 노출하지 않아 보간이 미래 결과를 미리 보여주지 않는다.
- 카메라 스프링은 렌더 호출 횟수가 아니라 소스 프레임 경과량으로 적분하고, 캔버스 backing store는 실제 픽셀 크기가 바뀔 때만 다시 할당한다.
- 무작위 미리보기 시드가 제한 시간 안에 결과 게이트를 만들지 못하면 0.75초 뒤 새 시드로 자동 재시도한다. 한 번의 실패가 정적 맵 화면으로 영구 고정되지 않는다.
- 재질 복원 후 고정 24경기의 평균 첫 도착은 31.0972초, 결승 진입 선두 우승률은 83.333%였다. 확장 72경기에서는 30.5241초·73.611%로 약 75% 목표 범위를 재확인했다.
- 위치 공정성 재검사는 5명·10명 각 120경기를 사용했다. 우승 수는 각각 `14 / 31 / 23 / 30 / 22`, `14 / 13 / 9 / 15 / 9 / 13 / 18 / 12 / 7 / 10`이며 8개 가설 모두 Holm 보정 뒤 유의하지 않았다. 가장 작은 raw p는 5명 가장자리 효과 `0.0532`, 보정 p는 `0.4258`이다.
- 아래 기존 1.2.1 벤치마크 수치는 정적 Body 재질 복원 전 기준선이며 현재 릴리스 판정에는 사용하지 않는다.

## 2026-07-26 1.2.1 FINAL MIX 시네마틱·불규칙 출발·제한형 추격

- 결과 선확정이 없는 1.2.0의 당첨 원칙과 실제 결승 통과 순서를 그대로 유지했다.
- `layoutSeed`로 익명 슬롯별 `-12~+12px` x 지터를 만들고 전체 평균을 0으로 맞췄다. 모든 마블의 y좌표는 같고 최소 36px 간격을 유지하며 같은 물리 스텝에 동시에 해제한다.
- 미도착 후미 마블에만 +y 방향으로 작용하는 제한형 추격 가속을 추가했다. 선두 격차 600px까지는 0, 600~1,000px은 smooth ramp, 1,000px 이상은 거리 계수 상한이다.
- 거리 계수 상한에서 실시간 2위는 기본 중력의 25%, 최하위는 32%를 최대 추가 힘으로 받고 중간 순위는 연속 보간한다.
- 후미 마블의 closing speed가 증가하면 힘을 감쇠해 600px/s 이상에서 0으로 만든다.
- 선두 감속, 완주자 대상 힘, 횡방향 추격력, 위치·속도 덮어쓰기, 당첨자·예정 결과·참가자 신원 참조는 금지했다.
- `FINAL MIX` 75~100% 구간에서 첫 완주 전 전체 1·2위 사이에 발생해 6프레임 유지·15px 격차로 확인된 물리 역전만 실제 `holdStartFrameIndex`의 0.3초 전부터 0.2초 후까지 재생하도록 했다. 첫 완주 이후의 잔여 순위 교체는 대상에서 제외했다.
- 역전 이벤트는 총 0.5초 동안 0.5배속·100% 확대(2.0배) 클로즈업으로 강조한다. 연출이 겹치면 최신 이벤트로 대상과 0.5초 지속 시간을 갱신한다.
- reduced-motion은 정상 속도·무확대 정적 큐를 사용한다. 시네마틱은 물리 프레임, 충돌, 완주 순서, 당첨 결과를 바꾸지 않는다.
- `podiumCount = min(3, 참가자 수)`, `resultGateCount = max(winnerCount, podiumCount)`로 당첨 기준과 결과 전환 기준을 분리했다.
- 2명 경기는 전원, 3명 이상 경기는 최소 3위가 실제 화면에 도착해야 한다. `resultGateCount`번째 도착이 화면에 나타난 뒤 사용자가 실제로 볼 수 있는 경기 화면을 3초 유지하고 결과로 전환한다.
- 결과 대기 중에도 물리 프레임과 실시간 순위표를 계속 진행하고, 미도착 경쟁자의 선두 교체를 `resultGateCount`까지 감지한다.
- 고정 24경기(5명·10명 각 12시드)의 평균 첫 도착은 28.7347초, 결승 진입 선두 우승률은 전체 70.833%, 5명 75.000%, 10명 66.667%였다.
- 결승 진입 상위 3명 유지율은 0.84722, 진입·최종 순위 Spearman 상관은 0.816919, 15프레임 카메라 지연 기준 확인 선두 교체는 경기당 6.25회였다.
- 선두와의 평균 격차는 441.29px, 경기별 p95 격차 평균은 1,104.49px, 경기별 최대 격차 평균은 1,383.63px, 절대 최대 격차는 2,566.51px, 1,000px 초과 프레임 비율은 12.079%였다.
- `benchmark:positions`를 추가해 5명·10명 각 120경기의 실제 시작 x 기준 위치 공정성을 검사했다. 5명 슬롯 우승 수는 `30 / 29 / 25 / 13 / 23`, 균등성·선형 x raw p는 `0.1046 / 0.0553`; 10명은 `8 / 16 / 14 / 9 / 12 / 14 / 9 / 11 / 15 / 12`, 균등성·선형 x·결합 raw p는 `0.7728 / 0.7768 / 0.9603`이었다.
- 독립 표본을 합친 각 220경기에서 5명은 `49 / 54 / 44 / 31 / 42`, raw p `0.1484 / 0.0838 / 0.1982`; 10명은 `13 / 25 / 16 / 26 / 23 / 23 / 19 / 23 / 28 / 24`, `0.4542 / 0.1050 / 0.2227`이었다.
- 수행한 위치 가설은 Holm 보정 뒤 모두 유의하지 않았다. 5명 기본 표본의 `slot-4` 저점과 선형 x `p=0.0553`은 감시 대상으로 남겼으며, 비유의가 위치 승률의 완전한 동일성을 증명하지는 않는다.
- 각 120경기는 screening으로 사용하고 정식 release gate는 5명·10명 각각 900경기를 권장한다.
- 밸런스·공정성 실험 대상은 5명 이상으로 정했다. 2~4명 경기는 기능 호환성만 보장하며 추격 계수·공정성 수치의 적용 대상에서 제외했다.

> 아래 1.2.0 항목은 실시간 순위·선두 거리 기반 힘이 없었던 당시 순수 물리 계약과 벤치마크를 보존한 역사 기록이다.

## 2026-07-26 1.2.0 물리 전용 경기·선택적 결승 반전

- 결과 선확정형, 결과 시드, 사전 우승자 대응 분기를 제거했다. 경기 전에 `raceSeed`로 참가자를 익명 출발 슬롯에 한 번 배정하고 실제 결승 통과 순서만으로 당첨자와 순위를 정한다.
- 참가자 슬롯 배정을 `createRaceSlotAssignment`로 분리해 시뮬레이션 전에 확정하고, `RaceSimulation.slotToCandidateId`가 해당 매핑을 보유하도록 변경했다. `buildRacePlan`은 시뮬레이션 매핑을 검증해 사용하며 별도 대응을 다시 만들지 않는다.
- 선두 거리 기반 추격 가속, 전역 횡풍, 공간 힘장을 삭제했다. 참가자·현재 순위·선두 거리·예정 결과를 읽는 힘은 더 이상 존재하지 않는다.
- 시간초과 안전장치는 첫 완주 목표보다 늦은 40초 이후에만 시작하는 전역 중력 상승으로 단순화했다. 같은 시각의 모든 미완주 마블에 같은 비율을 적용한다.
- 범퍼 접촉 뒤 마블 속도를 직접 덮어쓰던 능동 충격을 삭제하고 Matter.js 충돌 법선과 반발계수만 사용하도록 변경했다.
- 외곽 경계 이탈 시 마블 위치·속도를 강제로 되돌리던 보정을 제거하고, 60Hz 경기 스텝당 두 번의 120Hz 물리 서브스텝으로 고탄성 충돌의 터널링을 방지했다.
- 범퍼 반발계수를 이전보다 약 30% 높인 `1.22~1.32`로 조정했다. 회전 막대는 기준 속도를 높이고 `0.68~0.80` 반발계수를 적용해 옆으로 튕기는 충돌을 강화했다.
- 일부 직선 코스의 외벽을 범퍼와 같은 반발계수로 동작하는 탄성 벽으로 지정했다.
- 장애물의 역할을 공용 5색에 고정했다: 핫핑크=범퍼, 레몬=핀, 민트=가이드, 스카이=탄성 벽, 라벤더=회전 막대.
- 라이트·다크 맵 토큰을 분리하고 라이트 모드를 기본값으로 적용했다. 모드가 바뀌어도 장애물 의미 색과 참가자 테마는 유지한다.
- 결승 직전의 양측 점프 범퍼를 제거하고 `한쪽 위험 범퍼 → 같은 쪽 벽 방향 회전 막대 → 무장애 완만 수축부 → 60px 결승 통로`로 재설계했다.
- 결승 위험 장애물 옆에는 모든 회전 각도에서 마블 3개 이상이 통과하는 우회 폭을 영구 보장한다. 접촉한 개별 선두만 잠시 지연될 수 있고 전체 대열을 위로 되돌리거나 붙잡지 않는다.
- 최종 회전 막대 폭을 210px에서 244px로, 위험 경로 점유율 상한을 56%에서 60%로 조정했다. 위험 범퍼 `x=365`, `105×30px`, 각도 `0.18rad`와 회전 속도 `-0.023`은 유지했다.
- 최종 형상의 실제 위험 점유율은 57.83%이며 상시 우회 폭은 159.59px, 마블 지름 기준 5.32개다.
- 실제 프레임에서만 파생되는 지속 선두 교체, 접전, 결승 접근, 포토 피니시, 속도 트레일 연출을 추가했다.
- 다중 당첨 경기의 지속 선두 교체 감지를 첫 완주에서 끝내지 않고, 이미 도착한 마블을 제외한 미도착 경쟁자를 대상으로 목표 인원이 도착할 때까지 유지하도록 확장했다.
- 같은 경기 다시 보기에서 카메라 위치·속도·선두 후보와 일시 연출이 남지 않도록 재생 카메라 에포크를 추가했다.
- 결과 진입 즉시 같은 `runId`의 최소 이력을 저장하고, 실제 도착자 수가 증가할 때만 해당 이력을 갱신하도록 변경해 프레임별 중복 저장을 차단했다.
- 모바일 순위표는 상위 3행 밖에 있는 현재 첫 미도착 경쟁자를 항상 추가 표시하도록 보강했다.
- 매 프레임 바뀌는 상태 수치에서 `aria-live`를 제거하고 지속 선두 교체·접전·결승 접근·포토 피니시 같은 안정화된 사건만 라이브 알림으로 유지했다.
- 라이트·다크 테마별 외곽선을 핀·가이드·탄성 벽·범퍼·회전 막대에 적용해 공용 5색의 역할은 유지하면서 트랙과의 경계를 강화했다.
- 고정 24경기 벤치마크의 평균 첫 완주는 29.3792초, 결승 진입 선두 우승률은 75.00%, 우승자의 진입 평균 순위는 1.2917위, 진입 상위 3명 유지율은 81.94%, 진입·최종 순위 Spearman 상관은 0.79558, 진입 후 첫 완주는 3.5847초, 우승자 최대 역주행은 53.8266px였다.
- 같은 표본의 전체 장애물·범퍼·결승 위험 범퍼·회전 막대·결승 회전 막대 접촉 참가자 비율은 각각 `93.33 / 82.22 / 8.33 / 68.89 / 5.56%`였다.
- 전체 접촉, 결승 위험 범퍼, 결승 회전 막대 접촉 에피소드는 경기당 각각 `21.5417 / 1.1667 / 0.5417회`였다.
- 0.5초 확인 선두 교체는 경기당 6.1667회이며 0–25 / 25–50 / 50–75 / 75–100% 구간에서 각각 `2.375 / 1.3333 / 1.125 / 1.3333회` 발생했다.
- 이전 양측 범퍼 구조의 결승 진입 선두 우승률 20.8%, 진입·최종 순위 상관 0.366, 우승자 진입 평균 3.25위, 진입 후 첫 완주 4.84초와 비교하면 레이스 전반의 순위 의미를 회복하면서 목표한 25.00%의 마지막 반전 가능성을 남겼다.
- 확장 72경기(5명·10명 × 시드 0~35)의 결승 진입 선두 우승률은 전체 77.78%(56/72), 5명 75.00%, 10명 80.56%였다. Spearman 상관 0.8598, 상위 3명 유지율 86.57%, 진입 후 첫 완주 3.358초, 시간초과 0건을 확인했다.
- 2·5·10명 각 12시드의 경계 건전성 36경기에서 시간초과와 경계 6px 초과 이탈이 모두 0건이었다.
- 고정 표본의 10명 경기에서 10명 전원이 실제 결승선을 통과했다.
- 린트, TypeScript, 생산 빌드와 제품 자동 테스트 53/53을 통과했다.

> 아래 1.1.4 이하 항목은 당시 동작을 보존한 더 이전의 역사 기록이다.

## 2026-07-26 1.1.4 결승 범퍼 10% 축소

- 결승 좌우 막대 범퍼를 90×30px에서 81×27px로 축소했다.
- 범퍼의 바깥 끝은 기존 벽 겹침 위치에 고정하고 중심을 바깥쪽으로 4.5px씩 옮겨, 외측 끼임 방지는 유지하면서 입구를 60px에서 78px로 넓혔다.
- 동일 24경기에서 평균 첫 완주가 33.90초에서 32.24초로 단축됐다.
- 27,554개 오브젝트 쌍의 최소 간격 48px, 결승 범퍼 규칙 위반 0건, 네 구간 지속 역전 발생을 재확인했다.

## 2026-07-26 1.1.3 프리뷰 프레임 전환 안정화

- 배포 브라우저 검사에서 10초 프리뷰 계획이 교체되는 찰나 이전 프레임 인덱스가 새 배열 범위를 벗어나 준비 화면이 중단되는 경우를 발견했다.
- 프레임 선택을 0~마지막 프레임으로 양방향 보정하고, 비정상 숫자와 비어 있는 과도 상태에도 안전하게 대기하도록 변경했다.
- 음수·NaN·초과 인덱스와 빈 배열을 포함한 회귀 테스트를 추가했다.

## 2026-07-26 1.1.2 막대형 범퍼·충돌 섬광·구간별 역전 검증

- 원형 능동 범퍼 8개를 회전된 pill 형태의 막대 충돌체로 교체하고, 반발 벡터에 막대 각도의 횡방향 성분을 섞었다.
- 실제 Matter.js 충돌 접점을 프레임에 저장해 해당 위치에 짧은 섬광·확산 링·스파크를 그린다.
- 결승 좌우 범퍼 안쪽 끝은 60px 통로에 맞추고 바깥 끝은 벽 외측까지 연장해 바깥 주머니를 제거했다.
- 기존 1.1.1 소스를 별도로 재생하고 같은 24개 시드로 비교했다. 범퍼 접촉 참가자 비율 70.56→89.44%, 지속 역전 6.08→7.54회, 첫 완주 30.35→33.90초였다.
- 지속 역전은 0–25% 2.50회, 25–50% 1.125회, 50–75% 0.917회, 75–100% 3.00회로 모든 구간에서 확인했다.
- 맵 검사 27,554쌍에서 최소 통과 폭 48px, 요구 폭 36px, 위반 0건을 확인했다.

## 2026-07-26 1.1.1 구간 패턴·범퍼·추격 가속

- 물리 속도 계수와 장애물 크기를 유지한 채 코스 세로 길이를 9,000px에서 13,500px로 늘리고 목표 첫 완주를 20초에서 30초로 변경했다.
- 경기 구간 폭을 366~774px, 중심선을 x=250~660으로 조정해 수축·확장 편차와 좌우 이동량을 확대했다.
- 코스를 25% 단위의 `PIN DROP`, `BUMPER RUN`, `SQUEEZE GATES`, `FINAL MIX`로 나누고 각 구간의 핀·범퍼·회전 장치 구성이 서로 다르도록 재배치했다.
- 경기 화면과 준비 미리보기에 25·50·75% 경계, 구간명, 미세한 구간 색조를 표시했다.
- 일반 능동 범퍼 6개를 추가하고 충돌 공을 위쪽으로 튕겨 무리를 다시 섞도록 했다.
- 60px 결승 통로 직전 수축부의 좌우 벽에 점프 범퍼를 하나씩 연결해 공을 위쪽·안쪽으로 되돌리도록 했다.
- 선두와 360px 이상 벌어진 미완주 공은 물리 거리에 비례해 최대 1,350px까지 점진 가속을 받도록 했다.
- N번째 당첨자 공개 뒤에도 시뮬레이션 프레임을 끝까지 재생하고, 실제 도착한 참가자만 결과표에 순서대로 채우도록 변경했다.
- 정적인 전체 맵 미리보기 대신 10초짜리 실제 물리 미리보기를 재생하고, 회차마다 프리뷰 경기 시드와 출발 배치 시드를 함께 갱신하도록 했다.
- 범퍼 간격, 결승 범퍼 쌍, 네 구간 오브젝트 서명, 추격 함수 경계를 자동 테스트에 추가했다.

## 2026-07-26 1.1.0 Ex Lab Race 편성·대기·다중 당첨

- 전체 프로젝트명을 `Ex Lab`, 현재 게임명을 `Race`로 분리하고 특정 서비스 명칭을 제거했다.
- 본문 Pretendard와 제목용 표시 서체 토큰을 분리했다.
- 10개의 대비 검증 참가자 테마를 준비 명단, 미리보기, 공, 순위표, 결과에 연결했다.
- 전체 명단을 조당 최대 10명으로 균형 편성하고, 32명은 기본 8명씩 4조가 되도록 최소 조 수를 자동 보정했다.
- 동일 이름은 기본 차단하며 운영자 옵션으로만 허용한다.
- `방송 화면 열기 → 대기 → 경기 시작` 상태를 추가했다.
- 당첨 인원을 N명으로 설정하고 N번째 실제 결승 통과 뒤에만 결과 화면으로 전환한다.
- 풀 패키지의 순차 조 진행과 결과 종합 계약을 별도 시스템 명세에 기록했다.

## 2026-07-26 1.0.1 시드 기반 물리 변형

- 사용자의 의도를 축하·환호 효과가 아닌 경기 무작위성과 재미의 강화로 바로잡았다.
- 구현 전 `ready → generating → 3 → 2 → 1 → GO → running → result` 상태와 재현성·공정성 불변 조건을 문서화했다.
- 18개 후보를 검토하고 일반 회전막대 시작각·방향·속도, 결승 막대 시작각·속도, 전역 횡풍 3회, 공간 힘장 2곳, 중력·공·핀·벽 반발 편차를 적용했다.
- 결승 입구 막대는 시드 변형 뒤에도 항상 지정된 벽 방향으로 회전하며 모든 힘은 이름이 아니라 시간·공간·슬롯에만 작용한다.
- 기존 카운트다운의 거의 보이지 않던 `GO` 상태를 420ms 동안 표시하는 명시적 네 단계로 교체했다.
- 동일 시드 역학·순위 재현, 서로 다른 시드 변형, 결승 막대 방향, 카운트다운 전이를 포함해 자동 테스트를 17개로 늘렸다.
- 2·5·10명 각 16개 시드의 첫 완주 평균은 21.02초·22.80초·23.31초였고 물리 변형 코드는 각 표본에서 16개 모두 고유했다.
- 2·5·10명 각 20개 시드의 평균 선두 교체는 4.7회·9.9회·12.9회였다.
- 최소 코스 간격 37px, 간격·양쪽 벽 장애물·결승 막대 위반 0건을 재확인했다.
- 린트, TypeScript, 생산 빌드와 17개 테스트를 통과했다.
- 사용자 기준에 따라 기존 상태를 1.0.0으로 보고 이번 배포 버전을 1.0.1로 지정했다.

## 2026-07-26 60px 결승 통로와 지연형 선두 카메라
- 결승 통로의 실제 내부 폭과 체크 결승선을 96px에서 60px로 축소했다.
- 기존 중앙 회전 관문을 마지막 직선 입구의 왼쪽 벽 옆 120px 회전 막대로 교체했다.
- 입구 막대의 위쪽 접촉면이 인접 벽을 향하는 시계 방향 회전을 `rotatingBarTurnsTowardWall()`과 맵 검사기로 강제했다.
- 카메라 초점을 선두 3명 평균에서 단일 선두로 변경하고, 새 선두가 15프레임(0.5초) 유지될 때만 초점을 전환하는 상태를 추가했다.
- 세로 카메라 이동을 직접 위치 보간에서 가속도·속도 감쇠 기반 추적으로 변경했다. reduced-motion에서는 논리적 선두 확인은 유지하고 이동 관성만 제거한다.
- 25,739개 오브젝트 쌍의 최소 간격은 37px이며 간격·벽 장애물·결승 입구 회전 방향 위반은 모두 0건이다.
- 12개 시드 평균 첫 완주는 2인 21.2초, 5인 21.6초, 10인 23.8초다. 60px 병목으로 10인 평균 물리 완주는 8.5명이며 전원 완주 시드는 없었다.
- 배포 버전을 0.1.5로 올렸다.

## 2026-07-25 직선 구간 양측 벽 장애물 불변식
- 기존 맵 검사에서 `start-deck`, `central-release`, `wide-mix`, `final-gate`의 벽 부착 장애물이 누락된 것을 확인했다.
- 모든 경기용 직선 구간에 좌측·우측 벽 부착 장애물이 최소 1개씩 존재하도록 코스를 보강했다.
- 좌·우 장애물은 각각 우하향·좌하향으로 기울여 벽을 타는 마블을 항상 중앙 하류로 보낸다.
- `STRAIGHT_ZONES.requiresBilateralWallObstacles`를 단일 규칙으로 추가하고, 최소 통과 폭 검사와 같은 맵 검사 보고서에서 누락 구간을 즉시 실패시킨다.
- 폭 96px의 `finish-corridor`는 장애물을 두면 통로가 막히므로 명시적으로 제외하고, 바로 앞 `final-gate`에서 양쪽 벽 주행선을 차단한다.
- 25,739개 독립 오브젝트 쌍의 최소 표면 간격 37px, 통과 폭 위반 0건, 양측 벽 장애물 누락 0건을 확인했다.
- 장애물 증가에 맞춰 참가 인원별 초기 중력을 다시 조정했다. 12개 시드 평균 첫 완주는 2인 22.2초, 5인 23.1초, 10인 23.5초다.
- 배포 버전을 0.1.4로 올렸다.

## 2026-07-25 좌우 이동형 레이스 코스 재구성

- 고정 외벽과 내부 곡선 레일 구조를 폐기하고, 폭과 중심선이 다른 10개 직선 구간을 9개 S자 경계 전환부로 연결했다.
- 통로 폭은 740px에서 최종 120px까지 수축·확장을 반복하고, 중심선은 x=300~620 범위에서 좌우로 이동한다.
- `STRAIGHT_ZONES`를 경계 충돌체·미리보기·장애물 배치 검증의 단일 진실원으로 사용한다.
- 핀, 대형 섬, 중앙 선반, 회전 바는 직선 구간에만 배치했다. 회전 바는 전체 회전 반경까지 직선 구간 안에 들어오는지 검사한다.
- 벽 부착 디플렉터는 좌벽에서 우하향, 우벽에서 좌하향하도록 생성 함수와 자동 테스트로 고정했다.
- 연결된 장애물은 해당 벽과의 간격 검사만 제외하고, 반대 벽 및 다른 장애물과의 36px 최소 간격은 유지한다.
- 첫 검사에서 경계와 핀 사이 35px 위반 3건을 발견해 핀 좌표를 1~2px 안쪽으로 옮겼고, 최종 24,399개 형상 쌍에서 최소 37px·위반 0건을 확인했다.
- 새 경계 충돌로 경기 시간이 27~32초까지 늘어난 문제를 인원별 초기 중력 재보정으로 해결했다. 12개 시드 평균 첫 완주는 2인 21.9초, 5인 19.6초, 10인 20.4초다.
- 배포 버전을 0.1.3으로 올렸다.

## 2026-07-25 회전 막대 중심축 간격 모델

- 회전 막대의 최소 거리 검사를 막대 전체의 360° 외접원에서 `중심축에 반지름 15px 플레이어 공 하나가 놓인 상태`로 변경했다.
- 중심축 공과 다른 독립 오브젝트 사이에는 기존 36px 표면 간격을 그대로 강제한다.
- 실제 Matter.js 막대 길이·두께·회전 충돌은 바꾸지 않아 경기 물리와 연출은 유지하고, 회전 팔이 주변 오브젝트 가까이 지나가는 코스 설계를 허용한다.
- 검사 모델명과 반지름을 상수로 고정하고 자동 테스트로 회귀를 방지한다.
- 수정 완료 후 최신 비공개 배포를 갱신하고 해당 페이지를 인앱 브라우저에 여는 절차를 프로젝트 규칙으로 고정했다.
- 배포 버전을 0.1.2로 올렸다.

## 2026-07-25 결승 회전 관문과 96px 수축 통로

- 기존 마지막 직선 레일 3개와 중앙 핀 2개를 제거하고 결승 시퀀스를 `회전 관문 → 곡선 수축 → 좁은 직선 통로 → 결승선`으로 교체했다.
- 네 번째 회전 바는 폭 420px, 약 5.8초 회전 주기로 수축 곡선 입구 안쪽에서 연속 회전한다.
- 결승 통로의 표면 간 폭과 체크 라인 폭을 모두 96px로 통일했다.
- 연결된 좌·우 곡선과 직선 벽은 각각 하나의 충돌체 그룹으로 취급하고, 다른 모든 오브젝트와의 간격 검사는 그대로 유지했다.
- 전체 9,678개 형상 쌍의 최소 간격은 36px이며 위반은 0건이다.
- 16개 시드에서 첫 완주 평균은 5인 15.5초, 10인 21.3초였고, 10인 평균 물리 완주는 9.688명이었다.
- 결승 불변식 테스트를 추가하며 `FINISH_Y` 명시적 import 누락을 타입 검사에서 발견했고, 테스트 모듈 import를 보완한 뒤 전체 검증을 다시 통과했다.

## 2026-07-25 통과 폭·곡선 혼합 구간 재설계

- 공 반지름 15px를 기준으로 모든 독립 충돌체 사이에 최소 36px 표면 간격을 강제하는 `course-clearance.ts`를 추가했다.
- 정적 원, 회전 사각형, 외벽, 회전 바의 360° 운동 반경을 포함한 7,385개 오브젝트 쌍을 검사한다. 연결된 외곽 경계와 동일 곡선의 연결 조각만 단일 오브젝트로 묶어 예외 처리한다.
- 검사 도입 당시 발견된 23개 위반(외벽과 레일 겹침, 회전 반경과 핀 중첩 포함)을 코스 좌표와 길이 재편으로 0건까지 줄였다.
- 두 개의 반사이클로이드 레일과 `수축 → 완충 → 재확장` 곡선 구간을 실제 Matter.js 충돌체로 추가했다.
- 세 회전 막대를 각도 왕복 방식에서 서로 다른 방향·속도의 연속 360° 회전 방식으로 바꿨다.
- 넓어진 통로로 경기 시간이 지나치게 짧아지는 부작용을 참가 인원별 초기 중력 보정으로 조정했다. 16개 시드 표본에서 첫 완주 평균은 5인 17.6초, 10인 20.4초였다.

## 2026-07-25 비대칭 장거리 코스 조정

### 변경

- 물리 월드를 2,500에서 9,000 단위로 확장했다.
- 대칭 핀·합류 레일 중심 구성을 여덟 개 비대칭 구간, 세 개 회전 바, 두 종류의 슬라럼으로 교체했다.
- 물리 계산과 Canvas 렌더링이 같은 `course.ts` 코스 정의를 사용하도록 중복 좌표를 통합했다.
- reduced-motion에서도 전체 코스를 축소하지 않고 선두 세 명의 평균 구간으로 즉시 전환해 식별성을 유지했다.
- 준비 화면의 코스 미리보기를 실제 코스 정의에서 생성되는 SVG 개요도로 교체했다.

### 튜닝과 검증

- 긴 완만한 선반을 번갈아 배치한 첫 시안은 2인 경기에서 정체가 심하고 첫 완주가 50초를 넘어 폐기했다.
- 짧은 오프셋 장치와 긴 월드를 결합한 최종안은 5인 평균 약 20.4초, 10인 평균 약 18.8초, 2인 중앙값 약 22.2초에 첫 완주가 발생했다.
- 실제 10인 브라우저 경기 두 회는 카운트다운 포함 약 25초에 결과 화면으로 전환되었다.
- 참가자가 적을 때는 충돌에 의한 정체 해소가 적어 경기 시간 편차가 더 크다는 점을 남은 위험으로 기록했다.

## 2026-07-25 독립 마블 게임 기능 테스트 0.1.0

- 참가자 2~10명, 결과 선확정형과 물리 결과형, 시드 재현, 동일 경기 재생을 구현했다.
- Matter.js 고정 스텝 시뮬레이션과 Canvas 기반 자동 카메라를 구현했다.
- 11명 초과 입력 차단, 로컬 결과 기록, 효과음, 모바일 상위 3명 순위와 reduced-motion을 구현했다.
- 린트, 타입 검사, 빌드와 코어·SSR 자동 테스트를 통과했다.
