# Roulette 전면 UX 감사와 개선 명세

- 기준일: 2026-07-28
- 범위: 공통 명단 → Roulette 설계 → 방송 대기 → 추첨 → 결과 → 설계 복귀 → 복구/새 세션
- 대상 크기: 1600×900, 1440×900, 1024×768, 768×1024, 390×844
- 핵심 원칙: 결과는 물리 좌표로만 확정하고, UI는 결과를 조작하지 않으며, 사용자가 현재 세션의 상태와 다음 행동을 항상 예측할 수 있어야 한다.

## 1. 운영자가 수행하는 일

1. 공통 참가자 명단을 작성하거나 카페 댓글에서 가져온다.
2. 추첨 대상, 당첨 목표 인원, 회전/다트 연출, 제외와 가중치 규칙을 정한다.
3. 방송 화면을 열어 후보와 설정을 확인한 뒤 한 번씩 추첨한다.
4. 결과를 같은 조건으로 재연출하거나 다음 당첨자를 추첨한다.
5. 목표 인원에 도달하면 설계 화면으로 돌아가 세션을 이어가거나 명시적으로 새 세션을 만든다.

## 2. 정보 위계

1. 현재 단계와 실행 가능 여부
2. 이번 세션 진행 수 / 목표 수
3. 현재 후보와 제외 상태
4. 물리 추첨 화면과 방금 결과
5. 누적 결과
6. 고급 규칙, 실행 기록, 데이터 관리

## 3. 페이지·상태별 감사

### A. 공통 명단 편집

| 검사 항목 | 발견한 문제 | 개선 | 완료 조건 |
| --- | --- | --- | --- |
| 중복 이름 | 내장 편집기에서는 중복이 조용히 합쳐질 수 있었다. | 어떤 경로에서도 원문을 삭제하지 않고 구체적인 오류로 저장을 막는다. | 중복 줄과 해결 방법이 보이며 원문은 유지된다. |
| 긴 이름 | 40자 초과 이름이 사라지거나 엉뚱한 오류로 보일 수 있었다. | 줄 번호와 제한을 명시한다. | 41자 입력 시 손실 없이 저장이 막힌다. |
| 카페 파싱 | 오류 문장이 보조기술에 즉시 공지되지 않았다. | 오류를 alert/live region으로 연결한다. | 초점을 잃지 않고 실패 이유가 한 번 공지된다. |
| 게임 간 공유 | 저장 후 Roulette/Showdown 반영 시점이 불명확할 수 있다. | 저장 행동과 다음 시연부터의 반영을 명시한다. | 현재 연출은 변하지 않고 다음 주기부터 새 명단을 쓴다. |

### B. Roulette 설계 화면

| 검사 항목 | 발견한 문제 | 개선 | 완료 조건 |
| --- | --- | --- | --- |
| 당첨 목표 | 플랫폼 카탈로그는 복수 당첨을 지원한다고 했지만 목표 입력이 사라졌다. | 대상별 당첨 목표를 1명 단위로 설정한다. | 목표 수가 후보/재고보다 커지지 않는다. |
| 제외 상태 | 공통 명단 모드에서 `추첨 가능/제외/복귀`가 통째로 숨었다. | 편집은 공통 셸에 두고 Roulette 제외 상태는 설정 카드에 유지한다. | 10명 중 3명 제외 시 `7명 가능 · 3명 제외`와 복귀가 보인다. |
| 직전 세션 | 설계 복귀 시 몇 명을 뽑았는지와 이어갈 방법이 사라졌다. | 행동 막대를 직전 세션 허브로 확장한다. | `3/5 · 후보 7 · 마지막 이름`과 이어가기/새 세션이 보인다. |
| 새 세션 | 이어가기와 초기화가 같은 의미처럼 보였다. | 결과가 있는 세션 폐기는 확인 후 실행한다. | 버튼 라벨만으로 보존/삭제 범위를 예측할 수 있다. |
| 세로 길이 | 좁은 화면에서 설정 뒤에 행동 버튼이 밀렸다. | 설정 다음에 주요 행동, 그 뒤 미리보기 순으로 재배치하고 필요 시 행동을 sticky 처리한다. | 390×844에서 가로 스크롤 없이 핵심 CTA에 도달한다. |
| 긴 문구 | 규칙 설명이 상단을 밀어 미리보기와 CTA를 가렸다. | 요약 한 줄만 기본 노출하고 상세는 도구/고급 설정에서 연다. | 1024×768에서 제목이 두 줄 이상으로 폭주하지 않는다. |

### C. 방송 대기 화면

| 검사 항목 | 발견한 문제 | 개선 | 완료 조건 |
| --- | --- | --- | --- |
| 준비 증거 | 1024px에서 후보/원판/결과 3열이 서로 눌렸다. | 원판을 1순위, 후보와 결과를 증거 띠로 재배치한다. | 포인터, 원판, 후보 수, 목표 진행이 첫 화면에 보인다. |
| 후보 이름표 | 긴 이름과 많은 후보가 원판 경계를 덮었다. | 가용 둘레와 글자 길이에 따라 축약/간격을 조절한다. | 32명·긴 한글 이름에서도 원판/포인터를 가리지 않는다. |
| 상태 문구 | 같은 상태를 상단·무대·행동 도크에서 반복했다. | 상태는 한 곳, 진행 수는 결과 배지 한 곳에 둔다. | 동일 결과에 대한 live 공지가 한 번만 발생한다. |
| 명단 편집 | 방송 중 저장하면 세션이 설명 없이 종료됐다. | 활성 세션을 일시정지해 설계에서 복구 가능하게 한다. | 저장 후 기존 당첨·제외·목표가 보존된다. |

### D. 추첨 중

| 검사 항목 | 발견한 문제 | 개선 | 완료 조건 |
| --- | --- | --- | --- |
| 결과 확정 | 결과와 연출이 섞이면 재연출이 새 추첨처럼 동작할 위험이 있다. | 물리 클릭 좌표로 결과를 먼저 커밋하고 표현 토큰을 분리한다. | 같은 결과 재연출은 기록/재고/제외를 바꾸지 않는다. |
| 중복 실행 | 연타나 오래된 타이머가 다음 회차를 오염시킬 수 있다. | 상태 전이와 reveal token으로 오래된 콜백을 차단한다. | 추첨 중 CTA는 비활성화되고 결과는 한 번만 기록된다. |
| 시각적 가림 | 작은 화면에서 이름표/결과 레일이 원판 또는 다트를 가릴 수 있었다. | 뷰포트별 전용 배치와 안전 여백을 둔다. | 390×844와 1024×768에서 타깃 중심·포인터가 완전히 보인다. |
| 모션 접근성 | 축소 모션에서도 불필요한 이동이 남을 수 있다. | 이동 대신 짧은 페이드로 대체한다. | reduced-motion에서 결과와 조작을 기다릴 필요가 없다. |

### E. 회차 결과

| 검사 항목 | 발견한 문제 | 개선 | 완료 조건 |
| --- | --- | --- | --- |
| 진행 수 | 현재/목표와 남은 수가 없었다. | 누적 결과 제목에 `현재/목표`, 빈 슬롯에는 남은 수를 표시한다. | 첫 결과 후 `1/5`, 남은 4명이 즉시 보인다. |
| 다시 보기 | 카탈로그에는 지원으로 표시됐지만 실제 CTA가 없었다. | 동일 커밋 결과를 새 presentation id로 재연출한다. | 이력·제외·상품 재고·세션 결과 수가 모두 동일하다. |
| 다음 행동 | 목표 달성 전후 행동이 같았다. | 달성 전은 다음 추첨, 달성 후는 설계 복귀를 주 행동으로 둔다. | 사용자는 목표 달성 여부를 계산하지 않아도 된다. |
| 모바일 순서 | 후보 명단이 결과보다 먼저 길게 나와 결과와 CTA가 화면 아래로 밀렸다. | 모바일에서는 결과/CTA를 후보보다 먼저 둔다. | 첫 화면에 방금 결과와 다음 행동이 보인다. |
| 긴 결과명 | 말줄임만 있어 터치 환경에서 전체 이름을 알 수 없었다. | 모바일에서는 2줄까지 허용하고 전체 이름을 접근 가능한 이름으로 유지한다. | 40자 이름을 식별할 수 있다. |

### F. 설계 복귀와 복구

| 검사 항목 | 발견한 문제 | 개선 | 완료 조건 |
| --- | --- | --- | --- |
| 세션 소실 | `설계로`가 세션 객체를 삭제했다. | 활성 세션을 paused 슬롯으로 이동한다. | 결과·목표·대상·제외 상태가 유지된다. |
| 초점 소실 | 라이브 버튼이 언마운트되며 키보드 초점이 사라졌다. | 복귀 후 세션 허브 제목으로 초점을 이동한다. | 키보드 사용자가 즉시 현재 위치와 다음 행동을 안다. |
| 19명 이후 | 진행 도구가 18명만 렌더해 이후 참가자를 개별 복귀할 수 없었다. | 전체 명단을 스크롤 가능한 목록으로 제공한다. | 32번째 참가자도 찾아 제외/복귀할 수 있다. |

## 4. Keep / Delete / Merge / Move / Reveal

### Keep

- 클릭 좌표 기반 물리 결과 커밋
- 실행/표현 식별자로 오래된 콜백 차단
- 공통 명단과 스트리머 테마
- 누적 결과 기록과 상품 재고 규칙

### Delete

- 설계 복귀 시 세션을 무조건 삭제하는 동작
- 참가자 목록의 임의 18명 절단
- 같은 상태와 누적 수를 여러 곳에서 반복하는 문구
- 레이아웃을 떠받치기 위한 의미 없는 큰 홍보 문장

### Merge

- 설계 화면의 CTA와 직전 세션 복구 정보를 하나의 세션 허브로 통합
- 완료 행동을 `진행/목표 달성` 상태에 따라 하나의 행동 도크에서 분기

### Move

- 모바일 결과와 행동을 후보 명단보다 위로 이동
- 상세 추첨 규칙과 실행 정보는 도구/고급 영역으로 이동

### Reveal on demand

- 새 세션으로 폐기할 때만 영향 범위 확인
- 고급 후보 범위·가중치·상품 편집은 펼쳤을 때 표시
- 실행 ID와 상세 기록은 도구를 열었을 때 표시

## 5. 검토한 두 레이아웃

### 안 A — 복구 전용 카드

```text
[설정]
[직전 세션 카드: 3/5 | 이어가기 | 새 세션]
[미리보기]
[방송 화면 열기]
```

- 장점: 직전 세션이 잘 드러난다.
- 단점: 카드와 기존 CTA가 분리되어 모바일 세로 길이가 늘어난다.

### 안 B — 통합 세션 허브 (채택)

```text
[설정: 대상 | 목표 | 연출 | 명단 상태]
[세션 허브: 3/5 · 후보 7 · 마지막 이름]
[주 행동: 이어서 추첨] [새 세션]
[미리보기]
```

- 장점: 현재 상태와 다음 행동이 한 시선 흐름에 있고 신규 세션에서도 같은 자리를 쓴다.
- 단점: 정보 밀도를 낮추기 위해 문구 길이를 엄격히 제한해야 한다.

## 6. 상태·상호작용·모션

```text
설계
 ├─ 새 방송 열기 → 대기
 └─ 일시정지 세션 이어가기 → 대기 또는 회차 결과

대기 → 결과 잠금 → 물리 연출 → 회차 결과
                         └─ 같은 결과 다시 보기 ─┘

회차 결과
 ├─ 목표 전: 다음 추첨 → 대기
 ├─ 목표 달성: 설계로 → 설계 일시정지
 └─ 한 명 더: 목표 +1 → 대기

설계 일시정지
 ├─ 이어가기 → 회차 결과
 └─ 새 세션 → 확인 → 설계
```

- 재연출은 0.2초 안에 같은 결과를 다시 잠그고 물리 모션만 재생한다.
- 결과 도킹은 기본 0.2~0.3초, reduced-motion에서는 위치 이동 없이 짧은 페이드로 대체한다.
- 화면 전환 뒤 초점은 새 화면의 제목 또는 세션 허브로 이동한다.

## 7. 브라우저 수용 기준

- 1600×900, 1440×900: 원판/다트, 포인터, 이름표, 결과 레일이 겹치지 않는다.
- 1024×768: 후보·진행 수·결과·주 CTA가 한 화면 흐름 안에 있다.
- 768×1024, 390×844: 가로 스크롤이 없고 결과/CTA가 긴 후보보다 먼저 보인다.
- 참가자 2/5/10/18/32명과 빈 명단을 모두 검증한다.
- 40자 한글 이름과 40자 상품명을 검증한다.
- 대기/잠금/연출/히어로/도킹/완료/설계 복귀 상태를 모두 검증한다.
- 키보드만으로 명단 편집, 방송 열기, 추첨, 재연출, 이어가기, 새 세션이 가능하다.
- `prefers-reduced-motion`에서 핵심 정보와 CTA가 즉시 사용 가능하다.
- 재연출 전후 이력 수, 제외 수, 상품 재고, 세션 결과 수가 동일하다.

## 8. 구현 후 검증 결과

- 1600×900, 1440×900과 1024×768 설계/방송 화면은 Ex Lab 공통 헤더를 포함해 문서 세로·가로 넘침 없이 한 화면에 들어온다.
- 1024×768 방송 대기와 완료 화면은 공통 헤더 높이를 제외한 정확한 영역을 사용하며, 결과 도크의 두 번째 버튼 행까지 화면 안에 표시된다.
- 1600×900의 독립 방송용 40px 오프셋과 완료 상태의 `:has()` 높이 규칙이 임베디드 셸을 늘리던 충돌을 58px 공통 헤더 계약으로 차단했다.
- 같은 1600×900 대기 화면에서 숨긴 제목용 4행 그리드가 원판을 0px 행에 넣던 충돌을 단일 무대 행으로 바꿔 회전 룰렛과 다트를 실제 캡처에서 모두 확인했다.
- 768×1024 단계 바의 `명단 · 기록`은 한 줄을 유지하며, 390×844 대기 화면은 원판·행동·명단을 한 열의 전체 폭으로 표시한다.
- 360×640 완료 화면은 결과표와 행동 도크의 교차 영역이 0px이고, 결과·주 행동·보조 행동을 후보 명단보다 먼저 표시한다.
- 같은 결과를 다시 재생한 뒤에도 당첨자, 누적 결과 수와 제외 상태가 증가하지 않는다.
- 완료 세션을 다시 열면 다음 후보 명단이 아니라 직전 추첨에 실제로 사용한 8개 후보, 정지 transform과 `WIN` 당첨 표식을 동일하게 복원한다. 다트도 원판 transform, 부착 다트의 보드 기준 transform과 당첨 표식이 모두 일치했다.
- 완료 직후 초점은 완료 화면의 주 행동으로, 설계 복귀 후에는 `완료한 추첨` 세션 허브 제목으로 이동한다.
- 당첨자를 상품 추첨으로 전달해 라이브 화면을 완전히 닫은 뒤에는 준비 화면의 `상품 추가` 주 행동으로 초점이 이동한다.
- reduced-motion 즉시 정착, 결과 이름 단일 live 공지와 준비 화면 초점 복구는 소스 회귀 테스트로 고정했다.
- 전체 빌드와 145개 자동 테스트, ESLint, GitHub Pages 산출물 21개와 `/exlab/` base 검증이 통과했다.
- 운영 의존성은 `npm audit --omit=dev` 기준 취약점 0건이다.
- 실제 브라우저 콘솔의 오류·경고는 없었다.
